<Page x:Class="Hacked.Views.AccountDetailsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:utu="using:Uno.Toolkit.UI"
      xmlns:models="using:Hacked.Core.Models"
      mc:Ignorable="d"
      Background="{ThemeResource BackgroundBrush}">
  <Page.Resources>
    <utu:BoolToVisibilityConverter x:Key="BoolToVisibility" />
  </Page.Resources>

  <Grid utu:SafeArea.Insets="VisibleBounds">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <utu:NavigationBar Content="{Binding Account.Address}" Grid.Row="0">
      <utu:NavigationBar.PrimaryCommands>
        <AppBarButton Label="Clear New"
                      Command="{Binding ClearNewFlagsCommand}">
          <AppBarButton.Icon>
            <SymbolIcon Symbol="Clear" />
          </AppBarButton.Icon>
        </AppBarButton>
      </utu:NavigationBar.PrimaryCommands>
    </utu:NavigationBar>

    <TextBox Grid.Row="1"
             PlaceholderText="Filter breaches..."
             Text="{Binding FilterText, Mode=TwoWay}"
             Margin="12,8" />

    <ListView Grid.Row="2"
              ItemsSource="{Binding FilteredBreaches}"
              SelectionMode="None">
      <ListView.ItemTemplate>
        <DataTemplate x:DataType="models:Breach">
          <Grid Margin="12,8" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
              <TextBlock Text="{Binding Title}" FontWeight="Bold" />
              <TextBlock Text="{Binding Domain}" FontSize="12" Opacity="0.7" />
              <TextBlock>
                <Run Text="{Binding PwnCount}" />
                <Run Text=" accounts affected" />
              </TextBlock>
            </StackPanel>
            <Border Grid.Column="1"
                    Background="OrangeRed"
                    CornerRadius="4"
                    Padding="6,2"
                    VerticalAlignment="Center"
                    Visibility="{Binding IsNew, Converter={StaticResource BoolToVisibility}}">
              <TextBlock Text="NEW" Foreground="White" FontSize="11" FontWeight="Bold" />
            </Border>
          </Grid>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </Grid>
</Page>
