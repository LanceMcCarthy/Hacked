<UserControl x:Class="Hacked.Wpf.Views.MonitoredAccountsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Hacked.Wpf.Views"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="200">

    <UserControl.Resources>
        <DataTemplate x:Key="BreachLineItemTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <Image Source="{Binding LogoPath, Mode=OneWay}"
                       Margin="5" />

                <TextBlock Margin="10,0,0,0"
                           Grid.Column="1"
                           TextTrimming="CharacterEllipsis"
                           Text="{Binding Name, Mode=OneWay}" />
            </Grid>
        </DataTemplate>
        <HierarchicalDataTemplate x:Key="MonitoredAccountTemplate"
                                  ItemsSource="{Binding Breaches}"
                                  ItemTemplate="{StaticResource BreachLineItemTemplate}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Margin="0"
                           TextWrapping="Wrap"
                           Text="{Binding Address, Mode=OneWay}" />

                <TextBlock Text="{Binding Breaches.Count}"
                           Margin="5,0,0,0"
                           FontWeight="SemiBold"
                           Grid.Column="1" />
            </Grid>
        </HierarchicalDataTemplate>
    </UserControl.Resources>

    <Grid>
        <telerik:RadTreeView ItemsSource="{Binding Accounts}"
                             SelectedItem="{Binding SelectedAccount}"
                             ItemTemplate="{StaticResource MonitoredAccountTemplate}" />
    </Grid>
</UserControl>
