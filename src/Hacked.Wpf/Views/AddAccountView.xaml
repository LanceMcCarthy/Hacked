<UserControl x:Class="Hacked.Wpf.Views.AddAccountView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Hacked.Wpf.Views"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <telerik:RadWatermarkTextBox x:Name="EmailInput"
                                         WatermarkContent="Enter email address or username"
                                         KeyDown="EmailInput_OnKeyDown"
                                         TextChanged="TxtChanged"
                                         Grid.ColumnSpan="2"
                                         TabIndex="1"
                                         VerticalAlignment="Bottom"
                                         IsEnabled="{Binding IsBusy, Converter={StaticResource InvertBoolConverter}}"/>

            <Button x:Name="AddButton"
                    Content="Add"
                    Click="OkayButton_OnClick"
                    Grid.Row="1"
                    Grid.Column="0"
                    Margin="0,10,5,0"
                    IsEnabled="False"
                    VerticalAlignment="Bottom" />

            <Button x:Name="CancelButton"
                    Content="Cancel"
                    Click="CancelButton_OnClick"
                    Grid.Row="1"
                    Grid.Column="1"
                    HorizontalAlignment="Stretch"
                    Margin="5,10,0,0" />
        </Grid>
    </Grid>
</UserControl>
