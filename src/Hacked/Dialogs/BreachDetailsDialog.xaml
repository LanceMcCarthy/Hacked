<ContentDialog
    x:Class="Hacked.Dialogs.BreachDetailsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Hacked.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:Hacked.Converters"
    mc:Ignorable="d"
    d:DesignWidth="500"
    PrimaryButtonText="close"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick">

    <Grid>
        <!--<Grid.Resources>
            <converters:LogoConverter x:Key="LogoConverter" />
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <WebView x:Name="IconWebView" Height="150"/>-->
        <WebView x:Name="BreachDetailsWebView"
                 Width="300"
                 Height="300" />
    </Grid>
</ContentDialog>
