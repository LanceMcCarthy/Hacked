<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMaui="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    xmlns:telerikDataGrid="clr-namespace:Telerik.Maui.Controls.DataGrid;assembly=Telerik.Maui.Controls"
                    xmlns:telerikBottomSheet="clr-namespace:Telerik.Maui.Controls.BottomSheet;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Telerik.Styles.DataGrid">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <!-- Dependencies: Buttons.xaml -->

    <Shadow x:Key="DataGridDropDownButtonToolbarItemViewFocusedShadow" Offset="0, 0" Radius="2" Opacity="0.08" />

    <Style x:Key="DataGridIconButtonStyle" TargetType="telerikMauiControls:RadButton">
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="WidthRequest" Value="20" />
        <Setter Property="HeightRequest" Value="20" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderColor" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="Transparent" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha4}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha16}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="Transparent" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="DataGridHorizontalLineSeparatorStyle" TargetType="telerikMauiControls:RadBorder">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="HeightRequest" Value="1" />
    </Style>

    <Style x:Key="DataGridColumnCellContentStyle" TargetType="telerikDataGrid:DataGridTextCellAppearance">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="TextMargin" Value="9, 10" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="HoverTextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="SelectedTextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
    </Style>

    <Style x:Key="DataGridMouseHoverStyle" TargetType="telerikDataGrid:DataGridMouseHoverAppearance">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style x:Key="DataGridSelectionStyle" TargetType="telerikDataGrid:DataGridSelectionAppearance">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimarySubtleHoverColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style x:Key="DataGridCurrentCellStyle" TargetType="telerikDataGrid:DataGridCurrentCellAppearance">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BorderThickness" Value="2" />
    </Style>

    <Style x:Key="DataGridToggleRowDetailsCellStyle" TargetType="telerikDataGrid:DataGridToggleRowDetailsCellAppearance">
        <Setter Property="ButtonFontSize" Value="14" />
        <Setter Property="ButtonMargin" Value="9, 10" />
        <Setter Property="ButtonTextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="HoverButtonTextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="SelectedButtonTextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
    </Style>

    <Style x:Key="DataGridGroupHeaderStyle" TargetType="telerikDataGrid:DataGridGroupHeaderAppearance">
        <Setter Property="TextFontSize" Value="14" />
        <Setter Property="TextMargin" Value="9, 10" />
        <Setter Property="ExpandButtonText" Value="&#xe806;" />
        <Setter Property="CollapseButtonText" Value="&#xe80d;" />
        <Setter Property="ButtonTextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="TextFontAttributes" Value="Bold" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="AggregatesTextColor" Value="{DynamicResource RadSubtleColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSurfaceColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style x:Key="DataGridGroupFooterStyle" TargetType="telerikDataGrid:DataGridGroupFooterAppearance">
        <Setter Property="TextFontSize" Value="14" />
        <Setter Property="TextMargin" Value="9, 10" />
        <Setter Property="TextFontAttributes" Value="Bold" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSurfaceColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style x:Key="DataGridColumnHeaderStyle" TargetType="telerikDataGrid:DataGridColumnHeaderAppearance">
        <Setter Property="TextFontSize" Value="14" />
        <Setter Property="TextMargin" Value="9, 10" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSurfaceColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="HoverBackgroundColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="IndicatorColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="FilterIndicatorTextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="FilterIndicatorActiveTextColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="SortIndicatorColor" Value="{DynamicResource RadPrimaryColor}" />
    </Style>

    <Style x:Key="DataGridColumnFooterStyle" TargetType="telerikDataGrid:DataGridColumnFooterAppearance">
        <Setter Property="TextFontSize" Value="14" />
        <Setter Property="TextMargin" Value="9, 10" />
        <Setter Property="TextFontAttributes" Value="Bold" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSurfaceColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style x:Key="DataGridFrozenColumnsSplitterStyle" TargetType="telerikDataGrid:DataGridFrozenColumnsSplitterAppearance">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadBorderAltColor}" />
        <Setter Property="Width" Value="4" />
    </Style>

    <Style x:Key="DataGridLoadOnDemandRowAppearanceStyle" TargetType="telerikDataGrid:DataGridLoadOnDemandRowAppearance">
        <Setter Property="TextFontSize" Value="14" />
        <Setter Property="TextColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="IndicatorAnimationColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="BackgroundColor" Value="Transparent" />
    </Style>

    <Style x:Key="DataGridSearchPanelEntryStyle" TargetType="telerikMauiControls:RadEntry">
        <Setter Property="Placeholder" Value="{telerikMauiControls:Localize DataGrid_Search_SearchEntryPlaceholder}" />
        <Setter Property="PlaceholderColor" Value="{DynamicResource RadSubtleColor}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="ReturnType" Value="Search" />
        <Setter Property="ClearButtonVisibility" Value="WhileEditing" />
        <Setter Property="ReserveSpaceForErrorView" Value="False" />
        <Setter Property="VerticalOptions" Value="Center" />
    </Style>

    <Style x:Key="DataGridSearchPanelEntryStyle_Desktop" TargetType="telerikMauiControls:RadEntry" BasedOn="{StaticResource DataGridSearchPanelEntryStyle}">
        <Setter Property="WidthRequest" Value="200" />
    </Style>

    <Style x:Key="DataGridSearchPanelEntryToolbarItemStyle" TargetType="telerikMauiControls:EntryToolbarItemView">
        <Setter Property="EntryStyle" Value="{OnPlatform Default={StaticResource DataGridSearchPanelEntryStyle}, MacCatalyst={StaticResource DataGridSearchPanelEntryStyle_Desktop}, WinUI={StaticResource DataGridSearchPanelEntryStyle_Desktop}}" />
        <Setter Property="Padding" Value="{OnPlatform Default=0, WinUI=2}" />
        <Setter Property="ContentPadding" Value="{OnPlatform Default=0, WinUI=2, MacCatalyst=2}" />
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Android=222, iOS=38, WinUI=40, MacCatalyst=20}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Android=48, iOS=38, WinUI=40, MacCatalyst=20}" />
        <Setter Property="HorizontalOptions" Value="{OnPlatform Default=Fill, MacCatalyst=Start, WinUI=Start}" />
        <Setter Property="VerticalOptions" Value="Center" />
    </Style>

    <Style x:Key="DataGridSearchPanelCloseButtonToolbarItemStyle" TargetType="telerikMauiControls:ButtonToolbarItemView">
        <Setter Property="HorizontalOptions" Value="End" />
        <Setter Property="VerticalOptions" Value="Center" />
    </Style>

    <Style x:Key="DataGridSearchPanelSearchOptionsButtonToolbarItemStyle" TargetType="telerikMauiControls:DropDownButtonToolbarItemView">
        <Setter Property="DropDownVerticalOffset" Value="{OnPlatform Android=0, iOS=3, MacCatalyst=10, WinUI=2}" />
        <Setter Property="HorizontalOptions" Value="{OnPlatform Default=Start, Android=End, iOS=End}" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="MinimumWidthRequest" Value="30" />
        <Setter Property="MinimumHeightRequest" Value="30" />
        <Setter Property="ImageSpacing" Value="4" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadBaseColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadBaseHoverColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Opened" />
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
                <VisualStateGroup x:Name="FocusStates">
                    <VisualState x:Name="Unfocused" />
                    <VisualState x:Name="Focused">
                        <VisualState.Setters>
                            <Setter Property="Shadow" Value="{DynamicResource DataGridDropDownButtonToolbarItemViewFocusedShadow}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="DataGridSearchPanelSearchOptionsDropDownSeparatorStyle" TargetType="BoxView">
        <Setter Property="HeightRequest" Value="2" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadBorderColor}" />
    </Style>

    <Style x:Key="DataGridSearchPanelRadioButtonStyle_Mobile" TargetType="RadioButton">
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="ControlTemplate">
            <ControlTemplate>
                <Grid ColumnDefinitions="Auto, *"
                      ColumnSpacing="4">
                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CheckedStates">
                                <VisualState x:Name="Checked">
                                    <VisualState.Setters>
                                        <Setter TargetName="check" Property="Label.TextColor" Value="{DynamicResource RadPrimaryColor}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Unchecked" />
                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>
                    <Label x:Name="check"
                           Text="{x:Static telerikMaui:TelerikFont.IconOk}"
                           TextColor="Transparent"
                           FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                           FontSize="16"
                           WidthRequest="44"
                           HeightRequest="44"
                           HorizontalTextAlignment="Center"
                           VerticalTextAlignment="Center" />
                    <ContentPresenter Grid.Column="1"
                                      VerticalOptions="{TemplateBinding VerticalOptions}" />
                </Grid>
            </ControlTemplate>
        </Setter>
    </Style>

    <Style x:Key="DataGridSearchPanelRadioButtonStyle_Desktop" TargetType="RadioButton">
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Padding" Value="6, 0" />
        <Setter Property="ControlTemplate">
            <ControlTemplate>
                <Grid ColumnDefinitions="Auto, *"
                      ColumnSpacing="6">
                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CheckedStates">
                                <VisualState x:Name="Checked">
                                    <VisualState.Setters>
                                        <Setter TargetName="outerEllipse" Property="Ellipse.Fill" Value="{DynamicResource RadPrimaryColor}" />
                                        <Setter TargetName="outerEllipse" Property="Ellipse.Stroke" Value="{DynamicResource RadPrimaryColor}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Unchecked" />
                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>
                    <Grid>
                        <Ellipse x:Name="outerEllipse"
                                 Stroke="{DynamicResource RadBorderBrush}"
                                 Fill="{DynamicResource RadAppSurfaceBrush}"
                                 WidthRequest="16"
                                 HeightRequest="16"
                                 HorizontalOptions="Center"
                                 VerticalOptions="Center" />
                        <Ellipse x:Name="check"
                                 IsVisible="{TemplateBinding IsChecked}"
                                 Fill="{DynamicResource RadOnPrimaryBrush}"
                                 WidthRequest="6"
                                 HeightRequest="6"
                                 HorizontalOptions="Center"
                                 VerticalOptions="Center" />
                    </Grid>
                    <ContentPresenter Grid.Column="1" />
                </Grid>
            </ControlTemplate>
        </Setter>
    </Style>

    <telerikMauiControls:AreEqualToBoolConverter x:Key="DataGridSearchAreEqualToBoolConverter" />

    <ControlTemplate x:Key="DataGridSearchPanelTemplate">
        <telerikMauiControls:RadToolbar BackgroundColor="{DynamicResource RadBaseColor}"
                                        BindingContext="{TemplateBinding DataGrid.SearchSettings}"
                                        BorderThickness="1, 1, 1, 0"
                                        ContentPadding="{OnPlatform Android='16, 0', iOS='16, 3', MacCatalyst='12, 4', WinUI='8, 2'}"
                                        OverflowMode="Clip">
            <telerikMauiControls:EntryToolbarItem PlacementOptions="ToolStrip"
                                                  Text="{Binding IntermediateSearchText}"
                                                  telerikMauiControls:EntryExtensions.CompletedCommand="{Binding SearchEntryCompletedCommand}"
                                                  Style="{StaticResource DataGridSearchPanelEntryToolbarItemStyle}" />
            <telerikMauiControls:OptionsButtonToolbarItem Style="{StaticResource DataGridSearchPanelSearchOptionsButtonToolbarItemStyle}">
                <telerikMauiControls:OptionsButtonToolbarItem.ImageSource>
                    <FontImageSource Glyph="{x:Static telerikMaui:TelerikFont.IconDotsVert}"
                                     FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                                     Size="{OnPlatform Default=16, MacCatalyst=12}" />
                </telerikMauiControls:OptionsButtonToolbarItem.ImageSource>
                <telerikMauiControls:OptionsButtonToolbarItem.DropDownContentTemplate>
                    <ControlTemplate>
                        <telerikMauiControls:DropDownButtonToolbarItemViewContent ContentPadding="{OnPlatform MacCatalyst='5, 10'}">
                            <Grid RowDefinitions="Auto, Auto, Auto, Auto, Auto"
                                  RowSpacing="{OnPlatform MacCatalyst=8}">
                                <Grid ColumnDefinitions="Auto, *"
                                      ColumnSpacing="{OnPlatform Android=14, iOS=16, MacCatalyst=6, WinUI=7}"
                                      Padding="{OnPlatform Android=10, iOS='10, 11, 22, 11', MacCatalyst='7.5, 0', WinUI='0, 10'}"
                                      Margin="{OnPlatform WinUI='12, 0'}"
                                      telerikMauiControls:RadCheckBox.ToggleOnTap="{Binding Source={x:Reference caseSensitiveCheckBox}}">
                                    <telerikMauiControls:RadCheckBox x:Name="caseSensitiveCheckBox"
                                                                     IsChecked="{Binding CaseSensitive}" />
                                    <Label Grid.Column="1"
                                           Text="{telerikMauiControls:Localize DataGrid_Search_CaseSensitive}"
                                           FontSize="{OnPlatform iOS=17, MacCatalyst=13}" />
                                </Grid>
                                <BoxView Grid.Row="1"
                                         Style="{StaticResource DataGridSearchPanelSearchOptionsDropDownSeparatorStyle}" />
                                <Grid Grid.Row="2"
                                      RowDefinitions="Auto, Auto, Auto"
                                      RowSpacing="{OnPlatform MacCatalyst=8}"
                                      Padding="{OnPlatform WinUI='8, 0'}">
                                    <RadioButton IsChecked="{Binding TextMatchMode, Converter={StaticResource DataGridSearchAreEqualToBoolConverter}, ConverterParameter={x:Static telerikDataGrid:DataGridSearchTextMatchMode.Contains}}"
                                                 Content="{telerikMauiControls:Localize DataGrid_Search_Contains}"
                                                 Style="{OnPlatform Android={StaticResource DataGridSearchPanelRadioButtonStyle_Mobile},
                                                                    iOS={StaticResource DataGridSearchPanelRadioButtonStyle_Mobile},
                                                                    MacCatalyst={StaticResource DataGridSearchPanelRadioButtonStyle_Desktop}}" />
                                    <RadioButton Grid.Row="1"
                                                 IsChecked="{Binding TextMatchMode, Converter={StaticResource DataGridSearchAreEqualToBoolConverter}, ConverterParameter={x:Static telerikDataGrid:DataGridSearchTextMatchMode.StartsWith}}"
                                                 Content="{telerikMauiControls:Localize DataGrid_Search_StartsWith}"
                                                 Style="{OnPlatform Android={StaticResource DataGridSearchPanelRadioButtonStyle_Mobile},
                                                                    iOS={StaticResource DataGridSearchPanelRadioButtonStyle_Mobile},
                                                                    MacCatalyst={StaticResource DataGridSearchPanelRadioButtonStyle_Desktop}}" />
                                    <RadioButton Grid.Row="2"
                                                 IsChecked="{Binding TextMatchMode, Converter={StaticResource DataGridSearchAreEqualToBoolConverter}, ConverterParameter={x:Static telerikDataGrid:DataGridSearchTextMatchMode.WholeWord}}"
                                                 Content="{telerikMauiControls:Localize DataGrid_Search_WholeWord}"
                                                 Style="{OnPlatform Android={StaticResource DataGridSearchPanelRadioButtonStyle_Mobile},
                                                                    iOS={StaticResource DataGridSearchPanelRadioButtonStyle_Mobile},
                                                                    MacCatalyst={StaticResource DataGridSearchPanelRadioButtonStyle_Desktop}}" />
                                </Grid>
                                <BoxView Grid.Row="3"
                                         Style="{StaticResource DataGridSearchPanelSearchOptionsDropDownSeparatorStyle}" />
                                <Grid Grid.Row="4"
                                      ColumnDefinitions="Auto, *"
                                      ColumnSpacing="{OnPlatform Android=14, iOS=16, MacCatalyst=6, WinUI=7}"
                                      Padding="{OnPlatform Android=10, iOS='10, 11, 22, 11', MacCatalyst='7.5, 0', WinUI='0, 10'}"
                                      Margin="{OnPlatform WinUI='12, 0'}"
                                      telerikMauiControls:RadCheckBox.ToggleOnTap="{Binding Source={x:Reference applyFilterCheckBox}}">
                                    <telerikMauiControls:RadCheckBox x:Name="applyFilterCheckBox"
                                                                     IsChecked="{Binding ApplyFilter}" />
                                    <Label Grid.Column="1"
                                           Text="{telerikMauiControls:Localize DataGrid_Search_ApplyFilter}"
                                           FontSize="{OnPlatform iOS=17, MacCatalyst=13}" />
                                </Grid>
                            </Grid>
                        </telerikMauiControls:DropDownButtonToolbarItemViewContent>
                    </ControlTemplate>
                </telerikMauiControls:OptionsButtonToolbarItem.DropDownContentTemplate>
            </telerikMauiControls:OptionsButtonToolbarItem>
            <telerikMauiControls:LabelToolbarItem Text="{Binding ResultsSummary}"
                                                  IsVisible="{OnPlatform Android=False, iOS=False}" />
            <telerikMauiControls:ButtonToolbarItem PlacementOptions="ToolStrip"
                                                   Command="{Binding CloseSearchPanelCommand}"
                                                   IsVisible="{Binding IsCloseSearchPanelButtonVisible}"
                                                   Style="{StaticResource DataGridSearchPanelCloseButtonToolbarItemStyle}">
                <telerikMauiControls:LabelToolbarItem.ImageSource>
                    <FontImageSource Glyph="{x:Static telerikMaui:TelerikFont.IconCross}"
                                     FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                                     Size="16" />
                </telerikMauiControls:LabelToolbarItem.ImageSource>
            </telerikMauiControls:ButtonToolbarItem>
        </telerikMauiControls:RadToolbar>
    </ControlTemplate>

    <Style TargetType="telerikDataGrid:DataGridSearchPanel">
        <Setter Property="ControlTemplate" Value="{StaticResource DataGridSearchPanelTemplate}" />
    </Style>

    <DataTemplate x:Key="DataGridGroupingPanelItemTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{DynamicResource RadBaseColor}"
                                       BorderBrush="{DynamicResource RadBorderColor}"
                                       BorderThickness="1"
                                       CornerRadius="2"
                                       HeightRequest="30"
                                       Padding="8, 0">
            <Grid ColumnDefinitions="*, Auto"
                  ColumnSpacing="4">
                <Label Text="{Binding DisplayContent}"
                       TextColor="{DynamicResource RadOnAppSurfaceColor}"
                       FontSize="14"
                       VerticalOptions="Center" />
                <telerikMauiControls:RadButton Grid.Column="1"
                                               Style="{StaticResource DataGridIconButtonStyle}"
                                               Text="&#xe84e;"
                                               Command="{Binding RemoveCommand, Source={RelativeSource AncestorType={x:Type telerikDataGrid:DataGridGroupingPanel}}}"
                                               CommandParameter="{Binding}"
                                               VerticalContentAlignment="Center" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </DataTemplate>

    <telerikMauiControls:IsOfTypeConverter x:Key="IsColumnConverter" Type="{x:Type telerikDataGrid:DataGridColumn}" />

    <Style x:Key="DataGridGroupingPanelCollectionViewStyle_iOS" TargetType="VisualElement">
        <Setter Property="InputTransparent" Value="{Binding DataGrid.DraggedObject, Source={RelativeSource AncestorType={x:Type telerikDataGrid:DataGridGroupingPanel}}, Converter={StaticResource IsColumnConverter}}" />
    </Style>

    <ControlTemplate x:Key="DataGridGroupingPanelControlTemplate">
        <Grid ColumnDefinitions="Auto, *"
              BackgroundColor="Transparent">
            <Label Text="{TemplateBinding ActualHeaderText}"
                   FontSize="14"
                   TextColor="{DynamicResource RadOnAppSurfaceColor}"
                   VerticalOptions="Center"
                   Margin="{OnPlatform Default=8, Android='8, 12'}" />
            <CollectionView Grid.Column="1"
                            ItemTemplate="{Binding DataGrid.GroupingPanelItemTemplate, Source={RelativeSource AncestorType={x:Type telerikDataGrid:DataGridGroupingPanel}}, TargetNullValue={StaticResource DataGridGroupingPanelItemTemplate}}"
                            ItemsSource="{TemplateBinding DataGrid.GroupDescriptors}"
                            CanReorderItems="True"
                            HeightRequest="{OnPlatform Default=48, iOS=30, MacCatalyst=30}"
                            Margin="{OnPlatform Default=0, iOS='0, 9', MacCatalyst='0, 9'}"
                            Style="{OnPlatform iOS={StaticResource DataGridGroupingPanelCollectionViewStyle_iOS}, MacCatalyst={StaticResource DataGridGroupingPanelCollectionViewStyle_iOS}}">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Horizontal"
                                       ItemSpacing="8" />
                </CollectionView.ItemsLayout>
            </CollectionView>
        </Grid>
    </ControlTemplate>

    <Style TargetType="telerikDataGrid:DataGridGroupingPanel">
        <Setter Property="ControlTemplate" Value="{StaticResource DataGridGroupingPanelControlTemplate}" />
    </Style>

    <Style TargetType="telerikDataGrid:DataGridServicePanel">
        <Setter Property="Background" Value="{DynamicResource RadSurfaceColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
    </Style>

    <Style x:Key="DataGridAIPromptInputIconStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadSubtleColor}" />
    </Style>

    <Style x:Key="DataGridAIPromptInputTextInputStyle" TargetType="telerikMauiControls:RadTextInput">
        <Setter Property="PlaceholderColor" Value="{DynamicResource RadSubtleColor}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
    </Style>

    <Style x:Key="DataGridAIPromptInputClearButtonStyle" TargetType="telerikMauiControls:RadTemplatedButton">
        <Setter Property="WidthRequest" Value="20" />
        <Setter Property="HeightRequest" Value="20" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="TextColor" Value="{DynamicResource RadSubtleColor}" />
        <Setter Property="Content" Value="{x:Static telerikMaui:TelerikFont.IconAndroidClose}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="Focused" />
                    <VisualState Name="MouseOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="FocusedMouseOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="FocusedPressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="DataGridAIPromptInputSpeechToTextButtonStyle" TargetType="telerikMauiControls:RadSpeechToTextButton">
        <Setter Property="WidthRequest" Value="30" />
        <Setter Property="HeightRequest" Value="30" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderBrush}" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="Focused" />
                    <VisualState Name="MouseOver">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryHoverColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="FocusedMouseOver">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryHoverColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryActiveColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="FocusedPressed">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryActiveColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Listening">
                        <VisualState.Setters>
                            <Setter Property="Content" Value="{x:Static telerikMaui:TelerikFont.IconStopRecording}" />
                            <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="ListeningMouseOver">
                        <VisualState.Setters>
                            <Setter Property="Content" Value="{x:Static telerikMaui:TelerikFont.IconStopRecording}" />
                            <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="ListeningPressed">
                        <VisualState.Setters>
                            <Setter Property="Content" Value="{x:Static telerikMaui:TelerikFont.IconStopRecording}" />
                            <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Error">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadOnErrorColor}" />
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadErrorColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="Transparent" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="DataGridAIPromptInputSendButtonStyle" TargetType="telerikDataGrid:DataGridAISendButton">
        <Setter Property="Background" Value="{DynamicResource RadPrimaryBrush}" />
        <Setter Property="WidthRequest" Value="30" />
        <Setter Property="HeightRequest" Value="30" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
        <Setter Property="Content" Value="{x:Static telerikMaui:TelerikFont.IconSendMessage}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <Setter.Value>
                <VisualStateGroupList>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Normal">
                            <VisualState.Setters>
                                <Setter Property="Background" Value="{DynamicResource RadPrimaryBrush}" />
                                <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                                <Setter Property="Opacity" Value="1" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="Focused">
                            <VisualState.Setters>
                                <Setter Property="Background" Value="{DynamicResource RadPrimaryBrush}" />
                                <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                                <Setter Property="Opacity" Value="1" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="MouseOver">
                            <VisualState.Setters>
                                <Setter Property="Background" Value="{DynamicResource RadPrimaryHoverBrush}" />
                                <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                                <Setter Property="Opacity" Value="1" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="FocusedMouseOver">
                            <VisualState.Setters>
                                <Setter Property="Background" Value="{DynamicResource RadPrimaryHoverBrush}" />
                                <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                                <Setter Property="Opacity" Value="1" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="Pressed">
                            <VisualState.Setters>
                                <Setter Property="Background" Value="{DynamicResource RadPrimaryActiveBrush}" />
                                <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                                <Setter Property="Opacity" Value="1" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="FocusedPressed">
                            <VisualState.Setters>
                                <Setter Property="Background" Value="{DynamicResource RadPrimaryActiveBrush}" />
                                <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                                <Setter Property="Opacity" Value="1" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="Disabled">
                            <VisualState.Setters>
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                                <Setter Property="Opacity" Value="0.6" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState Name="Processing">
                            <VisualState.Setters>
                                <Setter Property="Content" Value="{x:Static telerikMaui:TelerikFont.IconStopRecording}" />
                                <Setter Property="Background" Value="{DynamicResource RadPrimaryBrush}" />
                                <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState Name="ProcessingPointerOver">
                            <VisualState.Setters>
                                <Setter Property="Content" Value="{x:Static telerikMaui:TelerikFont.IconStopRecording}" />
                                <Setter Property="Background" Value="{DynamicResource RadPrimaryHoverBrush}" />
                                <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState Name="ProcessingPressed">
                            <VisualState.Setters>
                                <Setter Property="Content" Value="{x:Static telerikMaui:TelerikFont.IconStopRecording}" />
                                <Setter Property="Background" Value="{DynamicResource RadPrimaryActiveBrush}" />
                                <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="telerikDataGrid:DataGridAISendButton" BasedOn="{StaticResource DataGridAIPromptInputSendButtonStyle}" />

    <VisualStateGroupList x:Key="DataGridAIPromptInput_VisualStates_Mobile">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Focused">
                <VisualState.Setters>
                    <Setter Property="telerikDataGrid:DataGridAIPromptInput.BackgroundColor" Value="{DynamicResource RadSurfaceAltColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikDataGrid:DataGridAIPromptInput.BorderBrush" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
                    <Setter Property="telerikDataGrid:DataGridAIPromptInput.Opacity" Value="0.6" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="DataGridAIPromptInput_VisualStates_Desktop">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="MouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikDataGrid:DataGridAIPromptInput.BackgroundColor" Value="{DynamicResource RadBaseHoverColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Focused">
                <VisualState.Setters>
                    <Setter Property="telerikDataGrid:DataGridAIPromptInput.BorderBrush" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikDataGrid:DataGridAIPromptInput.BorderBrush" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
                    <Setter Property="telerikDataGrid:DataGridAIPromptInput.Opacity" Value="0.6" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <Style x:Key="DataGridAIPromptInputStyle" TargetType="telerikDataGrid:DataGridAIPromptInput">
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=40, Android=42, iOS=62}" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="Padding" Value="{OnPlatform Default='8, 0', MacCatalyst='4,0', WinUI='4'}" />
        <Setter Property="SendButtonStyle" Value="{StaticResource DataGridAIPromptInputSendButtonStyle}" />
        <Setter Property="SpeechToTextButtonStyle" Value="{StaticResource DataGridAIPromptInputSpeechToTextButtonStyle}" />
        <Setter Property="ClearButtonStyle" Value="{StaticResource DataGridAIPromptInputClearButtonStyle}" />
        <Setter Property="IconStyle" Value="{StaticResource DataGridAIPromptInputIconStyle}" />
        <Setter Property="TextInputStyle" Value="{StaticResource DataGridAIPromptInputTextInputStyle}" />
        <Setter Property="VisualStateManager.VisualStateGroups"
                Value="{OnPlatform Android={StaticResource DataGridAIPromptInput_VisualStates_Mobile},
                                   iOS={StaticResource DataGridAIPromptInput_VisualStates_Mobile},
                                   MacCatalyst={StaticResource DataGridAIPromptInput_VisualStates_Desktop},
                                   WinUI={StaticResource DataGridAIPromptInput_VisualStates_Desktop}}" />
    </Style>
    <Style TargetType="telerikDataGrid:DataGridAIPromptInput" BasedOn="{StaticResource DataGridAIPromptInputStyle}" />

    <Style x:Key="DataGridAISuggestionsViewBaseLabelStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
    </Style>

    <Style x:Key="DataGridAISuggestionsViewSuggestedPromptsLabelStyle" TargetType="Label" BasedOn="{StaticResource DataGridAISuggestionsViewBaseLabelStyle}" />
    <Style x:Key="DataGridAISuggestionsViewRecentPromptsLabelStyle" TargetType="Label" BasedOn="{StaticResource DataGridAISuggestionsViewBaseLabelStyle}" />

    <Style x:Key="DataGridAISuggestionsViewBaseIconStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="Margin" Value="{OnPlatform Android='12, 16', iOS='9, 12, 16, 8', MacCatalyst='9, 13, 7, 7', WinUI='12'}" />
    </Style>

    <Style x:Key="DataGridAISuggestionsViewSuggestedPromptsIconStyle" TargetType="Label" BasedOn="{StaticResource DataGridAISuggestionsViewBaseIconStyle}" />
    <Style x:Key="DataGridAISuggestionsViewRecentPromptsIconStyle" TargetType="Label" BasedOn="{StaticResource DataGridAISuggestionsViewBaseIconStyle}" />

    <Style x:Key="DataGridAISuggestionsViewCollectionViewItemStyle" TargetType="telerikMauiControls:RadCollectionViewItemView" BasedOn="{StaticResource RadCollectionViewItemStyle}">
        <Setter Property="BorderThickness" Value="{OnPlatform Default='0, 0, 0, 1', MacCatalyst='0', WinUI='0'}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderBrush}" />
        <Setter Property="Padding" Value="{OnPlatform Android='40, 0', iOS='16, 0', MacCatalyst='33, 4, 30, 4', WinUI='38, 0'}" />
    </Style>

    <Style x:Key="DataGridAISuggestionsViewCollectionViewStyle" TargetType="telerikMauiControls:RadCollectionView">
        <Setter Property="ItemViewStyle" Value="{StaticResource DataGridAISuggestionsViewCollectionViewItemStyle}" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="VerticalOptions" Value="Start" />
        <Setter Property="Margin" Value="{OnPlatform Default='0', MacCatalyst='0, 0, 0, 4', WinUI='0, 0, 0, 4'}" />
        <Setter Property="MaximumHeightRequest" Value="{OnPlatform Android=144, iOS=132, MacCatalyst=124, WinUI=164}" />
    </Style>

    <Style x:Key="DataGridAISuggestionsViewSeparatorStyle" TargetType="Rectangle">
        <Setter Property="Fill" Value="{DynamicResource RadBorderBrush}" />
    </Style>

    <Style x:Key="DataGridAISuggestionsViewStyle" TargetType="telerikDataGrid:DataGridAISuggestionsView">
        <Setter Property="BackgroundColor" Value="{OnPlatform Default=Transparent, MacCatalyst={DynamicResource RadAppSurfaceColor}, WinUI={DynamicResource RadAppSurfaceColor}}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderBrush}" />
        <Setter Property="RecentPromptsLabelStyle" Value="{StaticResource DataGridAISuggestionsViewRecentPromptsLabelStyle}" />
        <Setter Property="SuggestedPromptsLabelStyle" Value="{StaticResource DataGridAISuggestionsViewSuggestedPromptsLabelStyle}" />
        <Setter Property="RecentPromptsIconStyle" Value="{StaticResource DataGridAISuggestionsViewRecentPromptsIconStyle}" />
        <Setter Property="SuggestedPromptsIconStyle" Value="{StaticResource DataGridAISuggestionsViewSuggestedPromptsIconStyle}" />
        <Setter Property="SeparatorStyle" Value="{StaticResource DataGridAISuggestionsViewSeparatorStyle}" />
        <Setter Property="RecentPromptsCollectionViewStyle" Value="{StaticResource DataGridAISuggestionsViewCollectionViewStyle}" />
        <Setter Property="SuggestedPromptsCollectionViewStyle" Value="{StaticResource DataGridAISuggestionsViewCollectionViewStyle}" />
    </Style>
    <Style TargetType="telerikDataGrid:DataGridAISuggestionsView" BasedOn="{StaticResource DataGridAISuggestionsViewStyle}" />

    <DataTemplate x:Key="DataGridAIPromptDropDownEmptyContentTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{OnPlatform Default={DynamicResource RadSurfaceAltColor}, MacCatalyst={DynamicResource RadAppSurfaceColor}, WinUI={DynamicResource RadAppSurfaceColor}}"
                                       BorderBrush="{DynamicResource RadBorderBrush}"
                                       BorderThickness="{OnPlatform Default=1, Android=0, iOS=0}"
                                       CornerRadius="{OnPlatform Android=12, iOS=12, MacCatalyst=5, WinUI=8}">
            <Grid RowDefinitions="Auto, *"
                  Padding="0, 24"
                  RowSpacing="16">
                <Label FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                       FontSize="56"
                       Text="{x:Static telerikMaui:TelerikFont.IconDocumentHistory}"
                       TextColor="{DynamicResource RadOnAppSurfaceColorAlpha16}"
                       HorizontalTextAlignment="Center" />
                <Label Grid.Row="1"
                       Text="{telerikMauiControls:Localize DataGrid_AI_PromptInputView_NoSuggestionsLabel}"
                       TextColor="{DynamicResource RadOnAppSurfaceColor}"
                       FontSize="20"
                       HorizontalTextAlignment="Center" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </DataTemplate>

    <Style x:Key="DataGridAIPromptViewHeaderLabelStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
    </Style>

    <Style x:Key="DataGridBottomSheetContentStyle" TargetType="telerikBottomSheet:BottomSheetContentView">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSurfaceAltColor}" />
    </Style>

    <Style TargetType="telerikDataGrid:DataGridAIBottomSheet">
        <Setter Property="HandleStyle">
            <Setter.Value>
                <Style TargetType="telerikBottomSheet:BottomSheetHandle">
                    <Setter Property="Margin" Value="{OnPlatform Android='0, 16, 0, 0', iOS='0, 15, 0, 0'}" />
                    <Setter Property="BackgroundColor" Value="{DynamicResource RadBorderColor}" />
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

    <DataTemplate x:Key="DataGridAIViewTemplate_Desktop">
        <telerikMauiControls:RadBorder BindingContext="{Binding AISettings, Source={RelativeSource AncestorType={Type telerikMauiControls:RadDataGrid}}}"
                                       BorderThickness="1, 1, 1, 0"
                                       BorderBrush="{DynamicResource RadBorderBrush}"
                                       BackgroundColor="{DynamicResource RadSurfaceAltColor}">
            <Grid Margin="0, 5">
                <telerikDataGrid:DataGridAIPromptDropDown WidthRequest="{OnPlatform Android=340, iOS=355, MacCatalyst=342, WinUI=350}"
                                                          HorizontalOptions="Center"
                                                          VerticalOptions="Center"
                                                          InputText="{Binding InputText}"
                                                          SuggestedPrompts="{Binding SuggestedPrompts}"
                                                          RecentPrompts="{Binding RecentPrompts}"
                                                          SubmitPromptOnSelection="{Binding SubmitPromptOnSelection}"
                                                          OpenOnFocus="{Binding OpenOnFocus}"
                                                          IsSuggestedPromptsVisible="{Binding IsSuggestedPromptsVisible}"
                                                          IsRecentPromptsVisible="{Binding IsRecentPromptsVisible}"
                                                          IsEmptyContentVisible="{Binding IsEmptyContentVisible}"
                                                          EmptyContentTemplate="{Binding EmptyContentTemplate}"
                                                          PromptInputStyle="{Binding PromptInputStyle}"
                                                          SuggestionsViewStyle="{Binding SuggestionsViewStyle}"
                                                          PromptRequestCommand="{Binding PromptRequestCommand}"
                                                          CancelPromptRequestCommand="{Binding CancelPromptRequestCommand}" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </DataTemplate>

    <DataTemplate x:Key="DataGridAIViewTemplate_Mobile">
        <telerikDataGrid:DataGridAIPromptView BindingContext="{Binding AISettings, Source={RelativeSource AncestorType={Type telerikMauiControls:RadDataGrid}}}"
                                              InputText="{Binding InputText}"
                                              SuggestedPrompts="{Binding SuggestedPrompts}"
                                              RecentPrompts="{Binding RecentPrompts}"
                                              SubmitPromptOnSelection="{Binding SubmitPromptOnSelection}"
                                              IsSuggestedPromptsVisible="{Binding IsSuggestedPromptsVisible}"
                                              IsRecentPromptsVisible="{Binding IsRecentPromptsVisible}"
                                              IsEmptyContentVisible="{Binding IsEmptyContentVisible}"
                                              EmptyContentTemplate="{Binding EmptyContentTemplate}"
                                              HeaderLabelStyle="{Binding BottomSheetContentHeaderLabelStyle}"
                                              PromptInputStyle="{Binding PromptInputStyle}"
                                              SuggestionsViewStyle="{Binding SuggestionsViewStyle}"
                                              PromptRequestCommand="{Binding PromptRequestCommand}"
                                              CancelPromptRequestCommand="{Binding CancelPromptRequestCommand}" />
    </DataTemplate>

    <Style x:Key="DataGridAIFloatingActionButtonIconStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadOnInverseColor}" />
    </Style>

    <Style x:Key="DataGridAIFloatingActionButtonStyle" TargetType="telerikDataGrid:DataGridAIFloatingActionButton">
        <Setter Property="Background" Value="{DynamicResource DataGridAIFloatingActionButtonBackground}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnInverseColor}" />
    </Style>

    <Style TargetType="telerikDataGrid:DataGridAISettings">
        <Setter Property="PromptInputStyle" Value="{StaticResource DataGridAIPromptInputStyle}" />
        <Setter Property="SuggestionsViewStyle" Value="{StaticResource DataGridAISuggestionsViewStyle}" />
        <Setter Property="EmptyContentTemplate" Value="{StaticResource DataGridAIPromptDropDownEmptyContentTemplate}" />
        <Setter Property="FloatingActionButtonStyle" Value="{OnPlatform Android={StaticResource DataGridAIFloatingActionButtonStyle}, iOS={StaticResource DataGridAIFloatingActionButtonStyle}}" />
        <Setter Property="BottomSheetContentStyle" Value="{StaticResource DataGridBottomSheetContentStyle}" />
        <Setter Property="BottomSheetContentHeaderLabelStyle" Value="{StaticResource DataGridAIPromptViewHeaderLabelStyle}" />
    </Style>

    <DataTemplate x:Key="DataGridColumnHeaderDragVisualTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{DynamicResource RadPrimaryColor}"
                                       CornerRadius="2"
                                       MinimumWidthRequest="{Binding ActualWidth}"
                                       MinimumHeightRequest="30"
                                       Padding="8, 4">
            <Label Text="{Binding HeaderText, FallbackValue='dragging...'}"
                   TextColor="{DynamicResource RadOnPrimaryColor}"
                   VerticalTextAlignment="Center" />
        </telerikMauiControls:RadBorder>
    </DataTemplate>

    <DataTemplate x:Key="DataGridColumnReorderIndicatorTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{DynamicResource RadPrimaryColor}"
                                       WidthRequest="4"
                                       HorizontalOptions="Start"
                                       InputTransparent="True" />
    </DataTemplate>

    <telerikMauiControls:NullOrEmptyToBoolConverter x:Key="NullOrEmptyToBoolConverter" />

    <Style x:Key="DataGridFilterControlRootBorderStyle" TargetType="telerikMauiControls:RadBorder">
        <Setter Property="Background" Value="{DynamicResource RadAppSurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="{OnPlatform Default=0, MacCatalyst=1, WinUI=1}" />
        <Setter Property="Padding" Value="{OnPlatform Default=0, Android='0, 8', iOS='0, 8'}" />
    </Style>

    <Style x:Key="DataGridFilterControlDistinctListBorderStyle" TargetType="telerikMauiControls:RadBorder">
        <Setter Property="Background" Value="{DynamicResource RadAppSurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style x:Key="DataGridFilterControlLabelStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="VerticalOptions" Value="Center" />
    </Style>

    <ControlTemplate x:Key="DataGridBooleanFilterControlTemplate">
        <telerikMauiControls:RadBorder Style="{StaticResource DataGridFilterControlRootBorderStyle}"
                                       WidthRequest="{OnPlatform MacCatalyst=300, WinUI=232}">
            <Grid RowDefinitions="*, Auto, Auto">
                <telerikDataGrid:DataGridDistinctValuesFilterView DistinctValues="{TemplateBinding DistinctValues}"
                                                                  IsVisible="{TemplateBinding DistinctValues, Converter={StaticResource NullOrEmptyToBoolConverter}}"
                                                                  VerticalOptions="Start"
                                                                  Margin="8" />
                <telerikMauiControls:RadButton Command="{TemplateBinding CloseCommand}"
                                               Text="&#xe84e;"
                                               Style="{StaticResource DataGridIconButtonStyle}"
                                               Margin="0, 8, 8, 8"
                                               HorizontalOptions="End"
                                               VerticalOptions="Start" />
                <OnPlatform x:TypeArguments="View">
                    <On Platform="Android, iOS">
                        <telerikMauiControls:RadBorder Grid.Row="1" Style="{StaticResource DataGridHorizontalLineSeparatorStyle}" />
                    </On>
                </OnPlatform>
                <Grid Grid.Row="2"
                      ColumnDefinitions="*, *"
                      ColumnSpacing="8"
                      Margin="0, 8, 0, 0"
                      Padding="8">
                    <telerikMauiControls:RadButton Text="{telerikMauiControls:Localize DataGrid_Filter_ResetFilter}"
                                                   Command="{TemplateBinding ResetFilterCommand}"
                                                   Style="{DynamicResource RadButtonStyle}" />
                    <telerikMauiControls:RadButton Grid.Column="1"
                                                   Text="{telerikMauiControls:Localize DataGrid_Filter_ApplyFilter}"
                                                   Command="{TemplateBinding ApplyFilterCommand}"
                                                   Style="{DynamicResource RadPrimaryButtonStyle}" />
                </Grid>
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>
    <Style TargetType="telerikDataGrid:DataGridBooleanFilterControl">
        <Setter Property="ControlTemplate" Value="{StaticResource DataGridBooleanFilterControlTemplate}" />
    </Style>

    <ControlTemplate x:Key="DataGridDateTimeFilterControlTemplate">
        <telerikMauiControls:RadBorder Style="{StaticResource DataGridFilterControlRootBorderStyle}"
                                       WidthRequest="{OnPlatform MacCatalyst=300, WinUI=232}">
            <Grid RowDefinitions="*, Auto, Auto">
                <ScrollView>
                    <VerticalStackLayout>
                        <telerikDataGrid:DataGridDistinctValuesFilterView DistinctValues="{TemplateBinding DistinctValues}"
                                                                          IsVisible="{TemplateBinding DistinctValues, Converter={StaticResource NullOrEmptyToBoolConverter}}"
                                                                          Margin="8" />
                        <OnPlatform x:TypeArguments="View">
                            <On Platform="Android, iOS">
                                <telerikMauiControls:RadBorder Style="{StaticResource DataGridHorizontalLineSeparatorStyle}" />
                            </On>
                        </OnPlatform>
                        <VerticalStackLayout Margin="8"
                                             Spacing="8">
                            <Label Text="{telerikMauiControls:Localize DataGrid_Filter_ShowRowsWithValueThat}"
                                   Style="{StaticResource DataGridFilterControlLabelStyle}" />
                            <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding DescriptorOperators}"
                                                             SelectedItem="{TemplateBinding FirstDescriptorOperator, Mode=TwoWay}"
                                                             IsClearButtonVisible="False"
                                                             Margin="0, 8" />
                            <telerikMauiControls:RadDatePicker Date="{TemplateBinding FirstDescriptorValue, Mode=TwoWay}"
                                                               MinimumDate="{TemplateBinding ActualMinimumDate}"
                                                               MaximumDate="{TemplateBinding ActualMaximumDate}" />
                            <OnPlatform x:TypeArguments="View">
                                <On Platform="Android, iOS">
                                    <telerikMauiControls:RadSegmentedControl ItemsSource="{TemplateBinding LogicalOperators}"
                                                                             SelectedIndex="{TemplateBinding LogicalOperatorIndex, Mode=TwoWay}" />
                                </On>
                                <On Platform="MacCatalyst, WinUI">
                                    <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding LogicalOperators}"
                                                                     SelectedItem="{TemplateBinding LogicalOperator, Mode=TwoWay}"
                                                                     IsClearButtonVisible="False"
                                                                     DropDownHeight="{OnPlatform MacCatalyst=44, WinUI=80}"
                                                                     Margin="0, 4" />
                                </On>
                            </OnPlatform>
                            <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding DescriptorOperators}"
                                                             SelectedItem="{TemplateBinding SecondDescriptorOperator, Mode=TwoWay}"
                                                             IsClearButtonVisible="False"
                                                             DropDownHeight="{OnPlatform MacCatalyst=132, WinUI=240}" />
                            <telerikMauiControls:RadDatePicker Date="{TemplateBinding SecondDescriptorValue, Mode=TwoWay}"
                                                               MinimumDate="{TemplateBinding ActualMinimumDate}"
                                                               MaximumDate="{TemplateBinding ActualMaximumDate}" />
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </ScrollView>
                <telerikMauiControls:RadButton Command="{TemplateBinding CloseCommand}"
                                               Text="&#xe84e;"
                                               Style="{StaticResource DataGridIconButtonStyle}"
                                               Margin="0, 8, 8, 8"
                                               HorizontalOptions="End"
                                               VerticalOptions="Start" />
                <OnPlatform x:TypeArguments="View">
                    <On Platform="Android, iOS">
                        <telerikMauiControls:RadBorder Grid.Row="1" Style="{StaticResource DataGridHorizontalLineSeparatorStyle}" />
                    </On>
                </OnPlatform>
                <Grid Grid.Row="2"
                      ColumnDefinitions="*, *"
                      ColumnSpacing="8"
                      Margin="0, 8, 0, 0"
                      Padding="8">
                    <telerikMauiControls:RadButton Text="{telerikMauiControls:Localize DataGrid_Filter_ResetFilter}"
                                                   Command="{TemplateBinding ResetFilterCommand}"
                                                   Style="{DynamicResource RadButtonStyle}" />
                    <telerikMauiControls:RadButton Grid.Column="1"
                                                   Text="{telerikMauiControls:Localize DataGrid_Filter_ApplyFilter}"
                                                   Command="{TemplateBinding ApplyFilterCommand}"
                                                   Style="{DynamicResource RadPrimaryButtonStyle}" />
                </Grid>
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>
    <Style TargetType="telerikDataGrid:DataGridDateTimeFilterControl">
        <Setter Property="ControlTemplate" Value="{StaticResource DataGridDateTimeFilterControlTemplate}" />
    </Style>

    <ControlTemplate x:Key="DataGridNumericFilterControlTemplate">
        <telerikMauiControls:RadBorder Style="{StaticResource DataGridFilterControlRootBorderStyle}"
                                       WidthRequest="{OnPlatform MacCatalyst=300, WinUI=232}">
            <Grid RowDefinitions="*, Auto, Auto">
                <ScrollView>
                    <VerticalStackLayout>
                        <telerikDataGrid:DataGridDistinctValuesFilterView DistinctValues="{TemplateBinding DistinctValues}"
                                                                          IsVisible="{TemplateBinding DistinctValues, Converter={StaticResource NullOrEmptyToBoolConverter}}"
                                                                          Margin="8" />
                        <OnPlatform x:TypeArguments="View">
                            <On Platform="Android, iOS">
                                <telerikMauiControls:RadBorder Style="{StaticResource DataGridHorizontalLineSeparatorStyle}" />
                            </On>
                        </OnPlatform>
                        <VerticalStackLayout Margin="8"
                                             Spacing="8">
                            <Label Text="{telerikMauiControls:Localize DataGrid_Filter_ShowRowsWithValueThat}"
                                   Style="{StaticResource DataGridFilterControlLabelStyle}" />
                            <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding DescriptorOperators}"
                                                             SelectedItem="{TemplateBinding FirstDescriptorOperator, Mode=TwoWay}"
                                                             IsClearButtonVisible="False"
                                                             DropDownHeight="{OnPlatform MacCatalyst=132, WinUI=240}" />
                            <telerikMauiControls:RadNumericInput Value="{TemplateBinding FirstDescriptorValue, Mode=TwoWay}" />
                            <OnPlatform x:TypeArguments="View">
                                <On Platform="Android, iOS">
                                    <telerikMauiControls:RadSegmentedControl ItemsSource="{TemplateBinding LogicalOperators}"
                                                                             SelectedIndex="{TemplateBinding LogicalOperatorIndex, Mode=TwoWay}" />
                                </On>
                                <On Platform="MacCatalyst, WinUI">
                                    <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding LogicalOperators}"
                                                                     SelectedItem="{TemplateBinding LogicalOperator, Mode=TwoWay}"
                                                                     IsClearButtonVisible="False"
                                                                     DropDownHeight="{OnPlatform MacCatalyst=44, WinUI=80}"
                                                                     Margin="0, 4" />
                                </On>
                            </OnPlatform>
                            <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding DescriptorOperators}"
                                                             SelectedItem="{TemplateBinding SecondDescriptorOperator, Mode=TwoWay}"
                                                             IsClearButtonVisible="False"
                                                             DropDownHeight="{OnPlatform MacCatalyst=132, WinUI=240}" />
                            <telerikMauiControls:RadNumericInput Value="{TemplateBinding SecondDescriptorValue, Mode=TwoWay}" />
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </ScrollView>
                <telerikMauiControls:RadButton Command="{TemplateBinding CloseCommand}"
                                               Text="&#xe84e;"
                                               Style="{StaticResource DataGridIconButtonStyle}"
                                               Margin="0, 8, 8, 8"
                                               HorizontalOptions="End"
                                               VerticalOptions="Start" />
                <OnPlatform x:TypeArguments="View">
                    <On Platform="Android, iOS">
                        <telerikMauiControls:RadBorder Grid.Row="1" Style="{StaticResource DataGridHorizontalLineSeparatorStyle}" />
                    </On>
                </OnPlatform>
                <Grid Grid.Row="2"
                      ColumnDefinitions="*, *"
                      ColumnSpacing="8"
                      Margin="0, 8, 0, 0"
                      Padding="8">
                    <telerikMauiControls:RadButton Text="{telerikMauiControls:Localize DataGrid_Filter_ResetFilter}"
                                                   Command="{TemplateBinding ResetFilterCommand}"
                                                   Style="{DynamicResource RadButtonStyle}" />
                    <telerikMauiControls:RadButton Grid.Column="1"
                                                   Text="{telerikMauiControls:Localize DataGrid_Filter_ApplyFilter}"
                                                   Command="{TemplateBinding ApplyFilterCommand}"
                                                   Style="{DynamicResource RadPrimaryButtonStyle}" />
                </Grid>
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>
    <Style TargetType="telerikDataGrid:DataGridNumericFilterControl">
        <Setter Property="ControlTemplate" Value="{StaticResource DataGridNumericFilterControlTemplate}" />
    </Style>

    <Style x:Key="DataGridMatchCaseButtonStyle" TargetType="telerikMauiControls:RadToggleButton">
        <Setter Property="Content" Value="{x:Static telerikMaui:TelerikFont.IconMatchCase}" />
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="WidthRequest" Value="{OnPlatform Android=44, iOS=36, MacCatalyst=30, WinUI=32}" />
        <Setter Property="HeightRequest" Value="{OnPlatform Android=44, iOS=36, MacCatalyst=30, WinUI=32}" />
    </Style>

    <ControlTemplate x:Key="DataGridTextFilterControlTemplate">
        <telerikMauiControls:RadBorder Style="{StaticResource DataGridFilterControlRootBorderStyle}"
                                       WidthRequest="{OnPlatform MacCatalyst=300, WinUI=232}">
            <Grid RowDefinitions="*, Auto, Auto">
                <ScrollView>
                    <VerticalStackLayout>
                        <telerikDataGrid:DataGridDistinctValuesFilterView DistinctValues="{TemplateBinding DistinctValues}"
                                                                          IsVisible="{TemplateBinding DistinctValues, Converter={StaticResource NullOrEmptyToBoolConverter}}"
                                                                          Margin="8" />
                        <OnPlatform x:TypeArguments="View">
                            <On Platform="Android, iOS">
                                <telerikMauiControls:RadBorder Style="{StaticResource DataGridHorizontalLineSeparatorStyle}" />
                            </On>
                        </OnPlatform>
                        <VerticalStackLayout Margin="8"
                                             Spacing="8">
                            <Label Text="{telerikMauiControls:Localize DataGrid_Filter_ShowRowsWithValueThat}"
                                   Style="{StaticResource DataGridFilterControlLabelStyle}" />
                            <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding DescriptorOperators}"
                                                             SelectedItem="{TemplateBinding FirstDescriptorOperator, Mode=TwoWay}"
                                                             IsClearButtonVisible="False"
                                                             DropDownHeight="{OnPlatform MacCatalyst=132, WinUI=240}" />
                            <Grid ColumnDefinitions="*, Auto"
                                  ColumnSpacing="{OnPlatform Default=5, Android=8, iOS=10}"
                                  Padding="{OnPlatform Default='0, 4', iOS='0, 8'}">
                                <telerikMauiControls:RadEntry Text="{TemplateBinding FirstDescriptorValue, Mode=TwoWay}"
                                                              Placeholder="{telerikMauiControls:Localize DataGrid_FilterWatermarkText}"
                                                              ReserveSpaceForErrorView="False" />
                                <telerikMauiControls:RadToggleButton Grid.Column="1"
                                                                     IsToggled="{TemplateBinding IsFirstDescriptorCaseSensitive}"
                                                                     Style="{StaticResource DataGridMatchCaseButtonStyle}"
                                                                     HorizontalOptions="End"
                                                                     VerticalOptions="Center" />
                            </Grid>
                            <OnPlatform x:TypeArguments="View">
                                <On Platform="Android, iOS">
                                    <telerikMauiControls:RadSegmentedControl ItemsSource="{TemplateBinding LogicalOperators}"
                                                                             SelectedIndex="{TemplateBinding LogicalOperatorIndex, Mode=TwoWay}" />
                                </On>
                                <On Platform="MacCatalyst, WinUI">
                                    <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding LogicalOperators}"
                                                                     SelectedItem="{TemplateBinding LogicalOperator, Mode=TwoWay}"
                                                                     IsClearButtonVisible="False"
                                                                     DropDownHeight="{OnPlatform MacCatalyst=44, WinUI=80}"
                                                                     Margin="0, 4" />
                                </On>
                            </OnPlatform>
                            <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding DescriptorOperators}"
                                                             SelectedItem="{TemplateBinding SecondDescriptorOperator, Mode=TwoWay}"
                                                             IsClearButtonVisible="False"
                                                             DropDownHeight="{OnPlatform MacCatalyst=132, WinUI=240}" />
                            <Grid ColumnDefinitions="*, Auto"
                                  ColumnSpacing="{OnPlatform Default=5, Android=8, iOS=10}"
                                  Padding="{OnPlatform Default='0, 4', iOS='0, 8'}">
                                <telerikMauiControls:RadEntry Text="{TemplateBinding SecondDescriptorValue, Mode=TwoWay}"
                                                              Placeholder="{telerikMauiControls:Localize DataGrid_FilterWatermarkText}"
                                                              ReserveSpaceForErrorView="False" />
                                <telerikMauiControls:RadToggleButton Grid.Column="1"
                                                                     IsToggled="{TemplateBinding IsSecondDescriptorCaseSensitive}"
                                                                     Style="{StaticResource DataGridMatchCaseButtonStyle}"
                                                                     HorizontalOptions="End"
                                                                     VerticalOptions="Center" />
                            </Grid>
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </ScrollView>
                <telerikMauiControls:RadButton Command="{TemplateBinding CloseCommand}"
                                               Text="&#xe84e;"
                                               Style="{StaticResource DataGridIconButtonStyle}"
                                               Margin="0, 8, 8, 8"
                                               HorizontalOptions="End"
                                               VerticalOptions="Start" />
                <OnPlatform x:TypeArguments="View">
                    <On Platform="Android, iOS">
                        <telerikMauiControls:RadBorder Grid.Row="1" Style="{StaticResource DataGridHorizontalLineSeparatorStyle}" />
                    </On>
                </OnPlatform>
                <Grid Grid.Row="2"
                      ColumnDefinitions="*, *"
                      ColumnSpacing="8"
                      Margin="0, 8, 0, 0"
                      Padding="8">
                    <telerikMauiControls:RadButton Text="{telerikMauiControls:Localize DataGrid_Filter_ResetFilter}"
                                                   Command="{TemplateBinding ResetFilterCommand}"
                                                   Style="{DynamicResource RadButtonStyle}" />
                    <telerikMauiControls:RadButton Grid.Column="1"
                                                   Text="{telerikMauiControls:Localize DataGrid_Filter_ApplyFilter}"
                                                   Command="{TemplateBinding ApplyFilterCommand}"
                                                   Style="{DynamicResource RadPrimaryButtonStyle}" />
                </Grid>
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>
    <Style TargetType="telerikDataGrid:DataGridTextFilterControl" ApplyToDerivedTypes="True">
        <Setter Property="ControlTemplate" Value="{StaticResource DataGridTextFilterControlTemplate}" />
    </Style>

    <ControlTemplate x:Key="DataGridTimeFilterControlTemplate">
        <telerikMauiControls:RadBorder Style="{StaticResource DataGridFilterControlRootBorderStyle}"
                                       WidthRequest="{OnPlatform MacCatalyst=300, WinUI=232}">
            <Grid RowDefinitions="*, Auto, Auto">
                <ScrollView>
                    <VerticalStackLayout>
                        <telerikDataGrid:DataGridDistinctValuesFilterView DistinctValues="{TemplateBinding DistinctValues}"
                                                                          IsVisible="{TemplateBinding DistinctValues, Converter={StaticResource NullOrEmptyToBoolConverter}}"
                                                                          Margin="8" />
                        <OnPlatform x:TypeArguments="View">
                            <On Platform="Android, iOS">
                                <telerikMauiControls:RadBorder Style="{StaticResource DataGridHorizontalLineSeparatorStyle}" />
                            </On>
                        </OnPlatform>
                        <VerticalStackLayout Margin="8"
                                             Spacing="8">
                            <Label Text="{telerikMauiControls:Localize DataGrid_Filter_ShowRowsWithValueThat}"
                                   Style="{StaticResource DataGridFilterControlLabelStyle}" />
                            <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding DescriptorOperators}"
                                                             SelectedItem="{TemplateBinding FirstDescriptorOperator, Mode=TwoWay}"
                                                             IsClearButtonVisible="False"
                                                             DropDownHeight="{OnPlatform MacCatalyst=132, WinUI=240}" />
                            <telerikMauiControls:RadTimePicker Time="{TemplateBinding FirstDescriptorValue, Mode=TwoWay}" />
                            <OnPlatform x:TypeArguments="View">
                                <On Platform="Android, iOS">
                                    <telerikMauiControls:RadSegmentedControl ItemsSource="{TemplateBinding LogicalOperators}"
                                                                             SelectedIndex="{TemplateBinding LogicalOperatorIndex, Mode=TwoWay}" />
                                </On>
                                <On Platform="MacCatalyst, WinUI">
                                    <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding LogicalOperators}"
                                                                     SelectedItem="{TemplateBinding LogicalOperator, Mode=TwoWay}"
                                                                     IsClearButtonVisible="False"
                                                                     DropDownHeight="{OnPlatform MacCatalyst=44, WinUI=80}"
                                                                     Margin="0, 4" />
                                </On>
                            </OnPlatform>
                            <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding DescriptorOperators}"
                                                             SelectedItem="{TemplateBinding SecondDescriptorOperator, Mode=TwoWay}"
                                                             IsClearButtonVisible="False"
                                                             DropDownHeight="{OnPlatform MacCatalyst=132, WinUI=240}" />
                            <telerikMauiControls:RadTimePicker Time="{TemplateBinding SecondDescriptorValue, Mode=TwoWay}" />
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </ScrollView>
                <telerikMauiControls:RadButton Command="{TemplateBinding CloseCommand}"
                                               Text="&#xe84e;"
                                               Style="{StaticResource DataGridIconButtonStyle}"
                                               Margin="0, 8, 8, 8"
                                               HorizontalOptions="End"
                                               VerticalOptions="Start" />
                <OnPlatform x:TypeArguments="View">
                    <On Platform="Android, iOS">
                        <telerikMauiControls:RadBorder Grid.Row="1" Style="{StaticResource DataGridHorizontalLineSeparatorStyle}" />
                    </On>
                </OnPlatform>
                <Grid Grid.Row="2"
                      ColumnDefinitions="*, *"
                      ColumnSpacing="8"
                      Margin="0, 8, 0, 0"
                      Padding="8">
                    <telerikMauiControls:RadButton Text="{telerikMauiControls:Localize DataGrid_Filter_ResetFilter}"
                                                   Command="{TemplateBinding ResetFilterCommand}"
                                                   Style="{DynamicResource RadButtonStyle}" />
                    <telerikMauiControls:RadButton Grid.Column="1"
                                                   Text="{telerikMauiControls:Localize DataGrid_Filter_ApplyFilter}"
                                                   Command="{TemplateBinding ApplyFilterCommand}"
                                                   Style="{DynamicResource RadPrimaryButtonStyle}" />
                </Grid>
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>
    <Style TargetType="telerikDataGrid:DataGridTimeFilterControl">
        <Setter Property="ControlTemplate" Value="{StaticResource DataGridTimeFilterControlTemplate}" />
    </Style>

    <DataTemplate x:Key="DistinctValueItemTemplate">
        <VerticalStackLayout>
            <HorizontalStackLayout telerikMauiControls:RadCheckBox.ToggleOnTap="{Binding Source={x:Reference checkBox}}"
                                   Margin="{OnPlatform Default=8, MacCatalyst=5}">
                <telerikMauiControls:RadCheckBox x:Name="checkBox"
                                                 IsChecked="{Binding IsSelected, Mode=TwoWay}" />
                <Label Text="{Binding Text}"
                       Style="{StaticResource DataGridFilterControlLabelStyle}"
                       Margin="{OnPlatform Default='8, 0, 0, 0', MacCatalyst='4, 0, 0, 0'}" />
            </HorizontalStackLayout>
            <telerikMauiControls:RadBorder Style="{StaticResource DataGridHorizontalLineSeparatorStyle}" />
        </VerticalStackLayout>
    </DataTemplate>
    <ControlTemplate x:Key="DataGridDistinctValuesFilterViewTemplate">
        <Grid RowDefinitions="Auto, Auto"
              RowSpacing="8">
            <HorizontalStackLayout telerikMauiControls:RadCheckBox.ToggleOnTap="{Binding Source={x:Reference checkBox}}">
                <telerikMauiControls:RadCheckBox x:Name="checkBox"
                                                 IsChecked="{TemplateBinding SelectAll, Mode=TwoWay}" />
                <Label Text="{telerikMauiControls:Localize DataGrid_DistinctValues_SelectAll}"
                       Style="{StaticResource DataGridFilterControlLabelStyle}"
                       Margin="{OnPlatform Default='8, 0, 0, 0', MacCatalyst='4, 0, 0, 0'}" />
            </HorizontalStackLayout>
            <telerikMauiControls:RadBorder Grid.Row="1"
                                           IsClippedToBounds="True"
                                           Style="{StaticResource DataGridFilterControlDistinctListBorderStyle}">
                <telerikMauiControls:RadItemsControl Background="Transparent"
                                                     ItemsSource="{TemplateBinding DistinctValues}"
                                                     ItemTemplate="{StaticResource DistinctValueItemTemplate}"
                                                     MinimumHeightRequest="{TemplateBinding DistinctValuesMinimumHeight}"
                                                     MaximumHeightRequest="{TemplateBinding DistinctValuesMaximumHeight}" />
            </telerikMauiControls:RadBorder>
        </Grid>
    </ControlTemplate>
    <Style TargetType="telerikDataGrid:DataGridDistinctValuesFilterView">
        <Setter Property="ControlTemplate" Value="{StaticResource DataGridDistinctValuesFilterViewTemplate}" />
    </Style>

    <Style x:Key="DataGridTypedColumnBaseStyle" TargetType="telerikDataGrid:DataGridTypedColumn" ApplyToDerivedTypes="True">
        <Setter Property="HeaderStyle" Value="{StaticResource DataGridColumnHeaderStyle}" />
        <Setter Property="CellContentStyle" Value="{StaticResource DataGridColumnCellContentStyle}" />
        <Setter Property="FooterStyle" Value="{StaticResource DataGridColumnFooterStyle}" />
    </Style>

    <Style TargetType="telerikDataGrid:DataGridTextColumn" ApplyToDerivedTypes="True" BasedOn="{StaticResource DataGridTypedColumnBaseStyle}">
        <Setter Property="CellEditorStyle">
            <Style TargetType="telerikMauiControls:RadEntry">
                <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=32, WinUI=30}" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                <Setter Property="BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
                <Setter Property="CornerRadius" Value="4" />
                <Setter Property="Margin" Value="2" />
                <Setter Property="ReserveSpaceForErrorView" Value="False" />
                <Setter Property="VerticalOptions" Value="Center" />
            </Style>
        </Setter>
    </Style>

    <Style TargetType="telerikDataGrid:DataGridBooleanColumn" ApplyToDerivedTypes="True" BasedOn="{StaticResource DataGridTypedColumnBaseStyle}">
        <Setter Property="CellEditorStyle">
            <Style TargetType="CheckBox">
                <Setter Property="Color" Value="{DynamicResource RadPrimaryColor}" />
                <Setter Property="Margin" Value="{OnPlatform Default='8, 2', Android='3, 2', WinUI='4, 2'}" />
                <Setter Property="HorizontalOptions" Value="Start" />
                <Setter Property="VerticalOptions" Value="Center" />
            </Style>
        </Setter>
    </Style>

    <Style TargetType="telerikDataGrid:DataGridComboBoxColumn" ApplyToDerivedTypes="True" BasedOn="{StaticResource DataGridTypedColumnBaseStyle}">
        <Setter Property="CellEditorStyle">
            <Style TargetType="telerikMauiControls:RadComboBox">
                <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                <Setter Property="Margin" Value="2" />
                <Setter Property="OpenOnFocus" Value="False" />
                <Setter Property="HorizontalOptions" Value="Fill" />
                <Setter Property="VerticalOptions" Value="Center" />
                <Setter Property="TextInputStyle">
                    <Style TargetType="telerikMauiControls:RadTextInput">
                        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=32, WinUI=30}" />
                        <Setter Property="Padding" Value="9, 0" />
                    </Style>
                </Setter>
            </Style>
        </Setter>
    </Style>

    <Style TargetType="telerikDataGrid:DataGridDateColumn" ApplyToDerivedTypes="True" BasedOn="{StaticResource DataGridTypedColumnBaseStyle}">
        <Setter Property="CellEditorStyle">
            <Style TargetType="telerikMauiControls:RadDatePicker">
                <Setter Property="MinimumDate" Value="1900,1,1" />
                <Setter Property="Margin" Value="2" />
                <Setter Property="HorizontalOptions" Value="Fill" />
                <Setter Property="VerticalOptions" Value="Center" />
                <Setter Property="DisplayLabelStyle">
                    <Style TargetType="Label">
                        <Setter Property="FontSize" Value="14" />
                        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                        <Setter Property="Margin" Value="{OnPlatform Default=8, Android='8, 6, 8, 12', WinUI='8, 4'}" />
                    </Style>
                </Setter>
            </Style>
        </Setter>
    </Style>

    <Style TargetType="telerikDataGrid:DataGridTimeColumn" ApplyToDerivedTypes="True" BasedOn="{StaticResource DataGridTypedColumnBaseStyle}">
        <Setter Property="CellEditorStyle">
            <Style TargetType="telerikMauiControls:RadTimePicker">
                <Setter Property="Margin" Value="2" />
                <Setter Property="HorizontalOptions" Value="Fill" />
                <Setter Property="VerticalOptions" Value="Center" />
                <Setter Property="DisplayStringFormat" Value="HH:mm" />
                <Setter Property="DisplayLabelStyle">
                    <Style TargetType="Label">
                        <Setter Property="FontSize" Value="14" />
                        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                        <Setter Property="Margin" Value="{OnPlatform Default=8, Android='8, 6, 8, 12', WinUI='8, 4'}" />
                    </Style>
                </Setter>
            </Style>
        </Setter>
    </Style>

    <Style TargetType="telerikDataGrid:DataGridNumericalColumn" ApplyToDerivedTypes="True" BasedOn="{StaticResource DataGridTypedColumnBaseStyle}">
        <Setter Property="CellEditorStyle">
            <Style TargetType="telerikMauiControls:RadNumericInput">
                <Setter Property="Margin" Value="2" />
                <Setter Property="VerticalOptions" Value="Center" />
                <Setter Property="TextInputStyle">
                    <Style TargetType="telerikMauiControls:NumericInputTextInput">
                        <Setter Property="FontSize" Value="14" />
                        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                        <Setter Property="BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
                        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=32, WinUI=30}" />
                        <Setter Property="Padding" Value="9, 0" />
                    </Style>
                </Setter>
            </Style>
        </Setter>
    </Style>

    <Style TargetType="telerikDataGrid:DataGridToggleRowDetailsColumn">
        <Setter Property="HeaderStyle" Value="{StaticResource DataGridColumnHeaderStyle}" />
        <Setter Property="CellContentStyle" Value="{StaticResource DataGridToggleRowDetailsCellStyle}" />
        <Setter Property="FooterStyle" Value="{StaticResource DataGridColumnFooterStyle}" />
    </Style>

    <Style TargetType="telerikDataGrid:DataGridTemplateColumn">
        <Setter Property="HeaderStyle" Value="{StaticResource DataGridColumnHeaderStyle}" />
        <Setter Property="FooterStyle" Value="{StaticResource DataGridColumnFooterStyle}" />
    </Style>

    <Style x:Key="RadDataGridStyle" TargetType="telerikMauiControls:RadDataGrid">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="GridLinesColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="MouseHoverStyle" Value="{StaticResource DataGridMouseHoverStyle}" />
        <Setter Property="SelectionStyle" Value="{StaticResource DataGridSelectionStyle}" />
        <Setter Property="CurrentCellStyle" Value="{StaticResource DataGridCurrentCellStyle}" />
        <Setter Property="GroupHeaderStyle" Value="{StaticResource DataGridGroupHeaderStyle}" />
        <Setter Property="GroupFooterStyle" Value="{StaticResource DataGridGroupFooterStyle}" />
        <Setter Property="GroupingPanelItemTemplate" Value="{StaticResource DataGridGroupingPanelItemTemplate}" />
        <Setter Property="FrozenColumnsSplitterStyle" Value="{StaticResource DataGridFrozenColumnsSplitterStyle}" />
        <Setter Property="LoadOnDemandRowStyle" Value="{StaticResource DataGridLoadOnDemandRowAppearanceStyle}" />
        <Setter Property="ColumnHeaderDragVisualTemplate" Value="{StaticResource DataGridColumnHeaderDragVisualTemplate}" />
        <Setter Property="ColumnReorderIndicatorTemplate" Value="{StaticResource DataGridColumnReorderIndicatorTemplate}" />
        <Setter Property="AIViewTemplate" Value="{OnPlatform Default={StaticResource DataGridAIViewTemplate_Mobile}, MacCatalyst={StaticResource DataGridAIViewTemplate_Desktop}, WinUI={StaticResource DataGridAIViewTemplate_Desktop}}" />
    </Style>
    <Style TargetType="telerikMauiControls:RadDataGrid" BasedOn="{StaticResource RadDataGridStyle}" />
</ResourceDictionary>
