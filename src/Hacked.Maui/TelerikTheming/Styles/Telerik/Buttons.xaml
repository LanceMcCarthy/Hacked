<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    xmlns:telerikMaui="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    x:Class="Telerik.Theming.Telerik.Styles.Buttons">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <Style x:Key="RadButtonStyle" TargetType="telerikMauiControls:RadButton">
        <Setter Property="TextColor" Value="{DynamicResource RadOnBaseColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadBaseColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="{OnPlatform Android='9, 11', iOS='9, 12.5', MacCatalyst='9, 6.5', WinUI='9, 5'}" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadBaseColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadBaseHoverColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadBaseActiveColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadBaseColor}" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>
    <Style TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource RadButtonStyle}" />

    <Style x:Key="RadTemplatedButtonStyle" TargetType="telerikMauiControls:RadTemplatedButton">
        <Setter Property="TextColor" Value="{DynamicResource RadOnBaseColor}" />
        <Setter Property="Background" Value="{DynamicResource RadBaseBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="{OnPlatform Android='9, 11', iOS='9, 12.5', MacCatalyst='9, 6.5', WinUI='9, 5'}" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="Shadow" Value="{x:Null}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadBaseBrush}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="MouseOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadBaseHoverBrush}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Focused" />

                    <VisualState Name="FocusedMouseOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadBaseHoverBrush}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadBaseActiveBrush}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="FocusedPressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadBaseActiveBrush}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadBaseBrush}" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>
    <Style TargetType="telerikMauiControls:RadTemplatedButton" BasedOn="{StaticResource RadTemplatedButtonStyle}" />

    <Style x:Key="RadPrimaryTemplatedButtonStyle" TargetType="telerikMauiControls:RadTemplatedButton" BasedOn="{StaticResource RadTemplatedButtonStyle}">
        <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
        <Setter Property="Background" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadPrimaryColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="MouseOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadPrimaryHoverColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Focused">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadPrimaryColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="FocusedMouseOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadPrimaryHoverColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadPrimaryActiveColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="FocusedPressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadPrimaryActiveColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadPrimaryColor}" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="RadFlatTemplatedButtonStyle" TargetType="telerikMauiControls:RadTemplatedButton" BasedOn="{StaticResource RadTemplatedButtonStyle}">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="Transparent" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="MouseOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadOnAppSurfaceColorAlpha4}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Focused">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="Transparent" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="FocusedMouseOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadOnAppSurfaceColorAlpha4}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadOnAppSurfaceColorAlpha16}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="FocusedPressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadOnAppSurfaceColorAlpha16}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="Transparent" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="RadToggleButtonStyle" TargetType="telerikMauiControls:RadToggleButton">
        <Setter Property="TextColor" Value="{DynamicResource RadOnBaseColor}" />
        <Setter Property="Background" Value="{DynamicResource RadBaseBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="{OnPlatform Android='9, 11', iOS='9, 12.5', MacCatalyst='9, 6.5', WinUI='9, 5'}" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="Shadow" Value="{x:Null}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnBaseColor}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadBaseBrush}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="{DynamicResource RadBorderColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="MouseOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnBaseColor}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadBaseHoverBrush}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="{DynamicResource RadBorderColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Focused">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnBaseColor}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadBaseBrush}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="{DynamicResource RadBorderColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="FocusedMouseOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnBaseColor}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadBaseHoverBrush}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="{DynamicResource RadBorderColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnBaseColor}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadBaseActiveBrush}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="{DynamicResource RadBorderColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="FocusedPressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnBaseColor}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadBaseActiveBrush}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="{DynamicResource RadBorderColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnBaseColor}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadBaseBrush}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="{DynamicResource RadBorderColor}" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Toggled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadPrimaryColor}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="Transparent" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="FocusedToggled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadPrimaryColor}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="Transparent" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="ToggledMouseOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadPrimaryHoverColor}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="Transparent" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="FocusedToggledMouseOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadPrimaryHoverColor}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="Transparent" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="ToggledPressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadPrimaryActiveColor}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="Transparent" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="FocusedToggledPressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadPrimaryActiveColor}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="Transparent" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="ToggledDisabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadPrimaryColor}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="Transparent" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Indeterminate">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnBaseColor}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadBaseBrush}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="{DynamicResource RadBorderColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="FocusedIndeterminate">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnBaseColor}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadBaseBrush}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="{DynamicResource RadBorderColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="IndeterminateMouseOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnBaseColor}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadBaseHoverBrush}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="{DynamicResource RadBorderColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="FocusedIndeterminateMouseOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnBaseColor}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadBaseHoverBrush}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="{DynamicResource RadBorderColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="IndeterminatePressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnBaseColor}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadBaseActiveBrush}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="{DynamicResource RadBorderColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="FocusedIndeterminatePressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnBaseColor}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadBaseActiveBrush}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="{DynamicResource RadBorderColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="IndeterminateDisabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnBaseColor}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadBaseBrush}" />
                            <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="{DynamicResource RadBorderColor}" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>
    <Style TargetType="telerikMauiControls:RadToggleButton" BasedOn="{StaticResource RadToggleButtonStyle}" />

    <Style x:Key="RadPrimaryButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource RadButtonStyle}">
        <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="BorderColor" Value="Transparent" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryHoverColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryActiveColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="RadOutlineButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource RadButtonStyle}">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="BackgroundColor" Value="Transparent" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadAppSurfaceColor}" />
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadAppSurfaceColor}" />
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="BackgroundColor" Value="Transparent" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="RadPrimaryOutlineButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource RadButtonStyle}">
        <Setter Property="TextColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadPrimaryColor}" />
                            <Setter Property="BackgroundColor" Value="Transparent" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadAppSurfaceColor}" />
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadAppSurfaceColor}" />
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadPrimaryColor}" />
                            <Setter Property="BackgroundColor" Value="Transparent" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="RadFlatButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource RadButtonStyle}">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderColor" Value="Transparent" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="Transparent" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha4}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha16}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="Transparent" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="RadPrimaryFlatButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource RadButtonStyle}">
        <Setter Property="TextColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderColor" Value="Transparent" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="Transparent" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryColorDarken4}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryColorDarken12}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="Transparent" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="RadDropDownButtonDropDownIndicatorStyle" TargetType="Label">
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="Text" Value="{x:Static telerikMaui:TelerikFont.IconDownDir}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
        <Style.Triggers>
            <DataTrigger TargetType="Label" Binding="{Binding IsOpen, Source={RelativeSource TemplatedParent}}"  Value="True">
                <Setter Property="Text" Value="{x:Static telerikMaui:TelerikFont.IconDownDir}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="RadDropDownButtonStyle" TargetType="telerikMauiControls:RadDropDownButton">
        <Setter Property="TextColor" Value="{DynamicResource RadOnBaseColor}" />
        <Setter Property="Background" Value="{DynamicResource RadBaseBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="DropDownBackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="DropDownBorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="DropDownCornerRadius" Value="0" />
        <Setter Property="DropDownVerticalOffset" Value="2" />
        <Setter Property="Padding" Value="{OnPlatform Android='9, 11', iOS='9, 12.5', MacCatalyst='9, 6.5', WinUI='9, 5'}" />
        <Setter Property="Shadow" Value="{x:Null}" />
        <Setter Property="DropDownIndicatorStyle" Value="{StaticResource RadDropDownButtonDropDownIndicatorStyle}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="MouseOver">
                        <VisualState.Setters>
                            <Setter Property="Background" Value="{DynamicResource RadBaseHoverBrush}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="FocusedMouseOver">
                        <VisualState.Setters>
                            <Setter Property="Background" Value="{DynamicResource RadBaseHoverBrush}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="Background" Value="{DynamicResource RadBaseActiveBrush}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="FocusedPressed">
                        <VisualState.Setters>
                            <Setter Property="Background" Value="{DynamicResource RadBaseActiveBrush}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Opened" />
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="Opacity" Value="0.6" />
                            <Setter Property="BorderBrush" Value="{DynamicResource RadBaseBrush}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>
    <Style TargetType="telerikMauiControls:RadDropDownButton" BasedOn="{StaticResource RadDropDownButtonStyle}" />

    <Style x:Key="RadSmartPasteButtonStyle" TargetType="telerikMauiControls:RadSmartPasteButton">
        <Setter Property="IconTextColor" Value="{DynamicResource RadOnBaseColor}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnBaseColor}" />
        <Setter Property="Background" Value="{DynamicResource RadBaseBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="{OnPlatform Android='9, 11', iOS='9, 12.5', MacCatalyst='9, 6.5', WinUI='9, 5'}" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="Shadow" Value="{x:Null}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadSmartPasteButton.Background" Value="{DynamicResource RadBaseBrush}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="MouseOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadSmartPasteButton.Background" Value="{DynamicResource RadBaseHoverBrush}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="FocusedMouseOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadSmartPasteButton.Background" Value="{DynamicResource RadBaseHoverBrush}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadSmartPasteButton.Background" Value="{DynamicResource RadBaseActiveBrush}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="FocusedPressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadSmartPasteButton.Background" Value="{DynamicResource RadBaseActiveBrush}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadSmartPasteButton.Background" Value="{DynamicResource RadBaseBrush}" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Processing">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadSmartPasteButton.Text" Value="{telerikMauiControls:Localize SmartPasteButton_CancelText}" />
                            <Setter Property="telerikMauiControls:RadSmartPasteButton.IconText" Value="{x:Static telerikMaui:TelerikFont.IconCancel}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="ProcessingPressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadSmartPasteButton.Text" Value="{telerikMauiControls:Localize SmartPasteButton_CancelText}" />
                            <Setter Property="telerikMauiControls:RadSmartPasteButton.IconText" Value="{x:Static telerikMaui:TelerikFont.IconCancel}" />
                            <Setter Property="telerikMauiControls:RadSmartPasteButton.Background" Value="{DynamicResource RadBaseActiveBrush}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="ProcessingFocused" />
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>
    <Style TargetType="telerikMauiControls:RadSmartPasteButton" BasedOn="{StaticResource RadSmartPasteButtonStyle}" />
</ResourceDictionary>
