<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    xmlns:telerikMaui="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    xmlns:telerikTheming="clr-namespace:Telerik.Maui.Controls.Theming;assembly=Telerik.Maui.Controls"
                    xmlns:telerikMauiCore="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    x:Class="Telerik.Theming.Platform.Styles.Buttons">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <telerikMauiControls:CornerRadiusToThicknessConverter x:Key="CornerRadiusToThicknessConverter" />
    <telerikMauiControls:NullOrEmptyToBoolConverter x:Key="NullOrEmptyToBoolConverter" />

    <Shadow x:Key="RadButtonShadow" Offset="0, 1" Radius="1.5" Brush="{DynamicResource RadOnAppSurfaceColor}" Opacity="0.2" />
    <Shadow x:Key="RadPrimaryButtonShadow" Offset="0, 1" Radius="3" Brush="{DynamicResource RadPrimaryColor}" Opacity="0.24" />

    <VisualStateGroupList x:Key="RadButtonVisualStates">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Pressed" />
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="RadButtonVisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadButton.BackgroundColor" Value="{DynamicResource RadBaseHoverColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadButton.TextColor" Value="{DynamicResource RadButtonPressedTextColor}" />
                    <Setter Property="telerikMauiControls:RadButton.BackgroundColor" Value="{DynamicResource RadBaseActiveColor}" />
                    <Setter Property="telerikMauiControls:RadButton.BorderColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha6}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadButton.BackgroundColor" Value="{DynamicResource RadBaseActiveColor}" />
                    <Setter Property="telerikMauiControls:RadButton.BorderColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha6}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <Style x:Key="RadButtonStyle" TargetType="telerikMauiControls:RadButton">
        <Setter Property="TextColor" Value="{OnPlatform Default={DynamicResource RadOnBaseColor}, MacCatalyst={DynamicResource RadPrimaryColor}}" />
        <Setter Property="BackgroundColor" Value="{OnPlatform Default={DynamicResource RadBaseColor}, iOS=Transparent, MacCatalyst=Transparent}" />
        <Setter Property="BorderColor" Value="{OnPlatform Default=Transparent, WinUI={DynamicResource RadButtonBorderBrushGradientStop1}}" />
        <Setter Property="BorderThickness" Value="{OnPlatform Default=0, WinUI=1}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=4, iOS=8, MacCatalyst=5}" />
        <Setter Property="VisualStateManager.VisualStateGroups" Value="{OnPlatform Default={StaticResource RadButtonVisualStates}, WinUI={StaticResource RadButtonVisualStates_WinUI}}" />
    </Style>
    <Style TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource RadButtonStyle}" />

    <ControlTemplate x:Key="RadTemplatedButton_ControlTemplate">
        <telerikMauiControls:RadEffectsView x:Name="PART_EffectsView"
                                            BackgroundColor="{TemplateBinding BackgroundColor}"
                                            Background="{TemplateBinding Background}"
                                            BorderColor="{TemplateBinding BorderColor}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="{TemplateBinding CornerRadius, Converter={StaticResource CornerRadiusToThicknessConverter}}"
                                            Padding="{TemplateBinding Padding}">
            <ContentPresenter telerikMauiControls:ContentPresenterUtils.Content="{TemplateBinding ActualContent}" />
        </telerikMauiControls:RadEffectsView>
    </ControlTemplate>

    <ControlTemplate x:Key="RadTemplatedButton_ControlTemplate_WinUI">
        <Grid>
            <telerikMauiControls:RadBorder InputTransparent="True"
                                           IsVisible="{TemplateBinding IsKeyboardFocused}"
                                           BorderColor="{DynamicResource RadOnAppSurfaceColor}"
                                           BorderThickness="2"
                                           CornerRadius="{TemplateBinding CornerRadius, Converter={StaticResource CornerRadiusToThicknessConverter}}"
                                           Margin="-3"
                                           telerikMauiControls:KeyboardHelper.IsTabStop="{TemplateBinding telerikMauiControls:KeyboardHelper.IsTabStop}" />
            <telerikMauiControls:RadEffectsView x:Name="PART_EffectsView"
                                                BackgroundColor="{TemplateBinding BackgroundColor}"
                                                Background="{TemplateBinding Background}"
                                                BorderColor="{TemplateBinding BorderColor}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="{TemplateBinding CornerRadius, Converter={StaticResource CornerRadiusToThicknessConverter}}"
                                                Padding="{TemplateBinding Padding}">
                <ContentPresenter telerikMauiControls:ContentPresenterUtils.Content="{TemplateBinding ActualContent}" />
            </telerikMauiControls:RadEffectsView>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="RadToggleButton_ControlTemplate">
        <telerikMauiControls:RadEffectsView x:Name="PART_EffectsView"
                                            BackgroundColor="{TemplateBinding BackgroundColor}"
                                            Background="{TemplateBinding Background}"
                                            BorderColor="{TemplateBinding BorderColor}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="{TemplateBinding CornerRadius, Converter={StaticResource CornerRadiusToThicknessConverter}}"
                                            Padding="{TemplateBinding Padding}">
            <ContentPresenter telerikMauiControls:ContentPresenterUtils.Content="{TemplateBinding ActualContent}" />
        </telerikMauiControls:RadEffectsView>
    </ControlTemplate>

    <ControlTemplate x:Key="RadToggleButton_ControlTemplate_WinUI">
        <Grid>
            <telerikMauiControls:RadBorder InputTransparent="True"
                                           IsVisible="{TemplateBinding IsKeyboardFocused}"
                                           BorderColor="{DynamicResource RadOnAppSurfaceColor}"
                                           BorderThickness="2"
                                           CornerRadius="{TemplateBinding CornerRadius, Converter={StaticResource CornerRadiusToThicknessConverter}}"
                                           Margin="-3"
                                           telerikMauiControls:KeyboardHelper.IsTabStop="{TemplateBinding telerikMauiControls:KeyboardHelper.IsTabStop}" />
            <telerikMauiControls:RadEffectsView x:Name="PART_EffectsView"
                                                BackgroundColor="{TemplateBinding BackgroundColor}"
                                                Background="{TemplateBinding Background}"
                                                BorderColor="{TemplateBinding BorderColor}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="{TemplateBinding CornerRadius, Converter={StaticResource CornerRadiusToThicknessConverter}}"
                                                Padding="{TemplateBinding Padding}">
                <ContentPresenter telerikMauiControls:ContentPresenterUtils.Content="{TemplateBinding ActualContent}" />
            </telerikMauiControls:RadEffectsView>
        </Grid>
    </ControlTemplate>

    <VisualStateGroupList x:Key="TemplatedButtonVisualStates_Android">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Pressed" />
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="TemplatedButtonVisualStates_iOS">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadButtonPressedTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="TemplatedButtonVisualStates_MacCatalyst">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Focused" />
            <VisualState Name="MouseOver" />
            <VisualState Name="FocusedMouseOver" />
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadButtonPressedTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="FocusedPressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadButtonPressedTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="TemplatedButtonVisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="MouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadBaseHoverBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="FocusedMouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadBaseHoverBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnBaseColorAlpha61}" />
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadBaseActiveBrush}" />
                    <Setter Property="telerikMauiControls:RadTemplatedButton.BorderBrush" Value="{DynamicResource RadToggleButtonBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadBaseActiveBrush}" />
                    <Setter Property="telerikMauiControls:RadTemplatedButton.BorderBrush" Value="{DynamicResource RadToggleButtonBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Focused" />
            <VisualState Name="FocusedPressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadButtonPressedTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <Style x:Key="RadTemplatedButtonStyle" TargetType="telerikMauiControls:RadTemplatedButton">
        <Setter Property="ControlTemplate" Value="{OnPlatform Default={StaticResource RadTemplatedButton_ControlTemplate}, WinUI={StaticResource RadTemplatedButton_ControlTemplate_WinUI}}" />
        <Setter Property="telerikTheming:RadEffects.RippleMode" Value="Pulse" />
        <Setter Property="telerikTheming:RadEffects.RippleColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha12}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnBaseColor}" />
        <Setter Property="Background" Value="{DynamicResource RadButtonBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadButtonBorderBrush}" />
        <Setter Property="BorderThickness" Value="{OnPlatform Default=0, MacCatalyst=0.5, WinUI=1}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=4, iOS=8, MacCatalyst=5}" />
        <Setter Property="Padding" Value="{OnPlatform Android='8, 7', iOS='10, 9.5', MacCatalyst='8, 1.5', WinUI='11, 5, 11, 6'}" />
        <Setter Property="HorizontalTextAlignment" Value="Center" />
        <Setter Property="VerticalTextAlignment" Value="Center" />
        <Setter Property="Shadow" Value="{OnPlatform Default={x:Null}, MacCatalyst={DynamicResource RadButtonShadow}}" />
        <Setter Property="VisualStateManager.VisualStateGroups"
                Value="{OnPlatform Android={StaticResource TemplatedButtonVisualStates_Android},
                                   iOS={StaticResource TemplatedButtonVisualStates_iOS},
                                   MacCatalyst={StaticResource TemplatedButtonVisualStates_MacCatalyst},
                                   WinUI={StaticResource TemplatedButtonVisualStates_WinUI}}" />
    </Style>
    <Style TargetType="telerikMauiControls:RadTemplatedButton" BasedOn="{StaticResource RadTemplatedButtonStyle}" />
    <VisualStateGroupList x:Key="ToggleButtonVisualStates_Android">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Pressed" />
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Toggled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadPrimaryBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ToggledPressed">
                <VisualState.Setters>
                    <Setter Property="telerikTheming:RadEffects.RippleColor" Value="{DynamicResource RadAppSurfaceColorAlpha12}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadToggleButtonToggledPressedBackground}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ToggledDisabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadPrimaryButtonDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadToggleButtonToggledDisabledBackground}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Indeterminate" />
            <VisualState Name="IndeterminatePressed" />
            <VisualState Name="IndeterminateDisabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="ToggleButtonVisualStates_iOS">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadButtonPressedTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Toggled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadPrimaryBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ToggledPressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadPrimaryButtonPressedBackground}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ToggledDisabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadPrimaryButtonDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadToggleButtonToggledDisabledBackground}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Indeterminate" />
            <VisualState Name="IndeterminatePressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadButtonPressedTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="IndeterminateDisabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="ToggleButtonVisualStates_MacCatalyst">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Focused" />
            <VisualState Name="MouseOver" />
            <VisualState Name="FocusedMouseOver" />
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadButtonPressedTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="FocusedPressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadButtonPressedTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Toggled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadPrimaryButtonGradientBackground}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Shadow" Value="{DynamicResource RadPrimaryButtonShadow}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="FocusedToggled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadPrimaryButtonGradientBackground}" /> 
                    <Setter Property="telerikMauiControls:RadToggleButton.Shadow" Value="{DynamicResource RadPrimaryButtonShadow}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ToggledMouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadPrimaryButtonGradientBackground}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Shadow" Value="{DynamicResource RadPrimaryButtonShadow}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="FocusedToggledMouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadPrimaryButtonGradientBackground}" /> 
                    <Setter Property="telerikMauiControls:RadToggleButton.Shadow" Value="{DynamicResource RadPrimaryButtonShadow}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ToggledPressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnPrimaryColorAlpha60}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadPrimaryButtonPressedGradientBackground}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Shadow" Value="{DynamicResource RadPrimaryButtonShadow}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="FocusedToggledPressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnPrimaryColorAlpha60}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadPrimaryButtonPressedGradientBackground}" /> 
                    <Setter Property="telerikMauiControls:RadToggleButton.Shadow" Value="{DynamicResource RadPrimaryButtonShadow}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ToggledDisabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadPrimaryButtonDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadBaseBrush}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Shadow" Value="{DynamicResource RadButtonShadow}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Indeterminate" />
            <VisualState Name="FocusedIndeterminate" />
            <VisualState Name="IndeterminateMouseOver" />
            <VisualState Name="FocusedIndeterminateMouseOver" />
            <VisualState Name="IndeterminatePressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadButtonPressedTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="FocusedIndeterminatePressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadButtonPressedTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="IndeterminateDisabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="ToggleButtonVisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Focused" />
            <VisualState Name="MouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadBaseHoverBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="FocusedMouseOver" >
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadBaseHoverBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnBaseColorAlpha61}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadBaseActiveBrush}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="{DynamicResource RadToggleButtonBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="FocusedPressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnBaseColorAlpha61}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadBaseActiveBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadBaseActiveBrush}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="{DynamicResource RadToggleButtonBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Toggled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadPrimaryBrush}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="{DynamicResource RadPrimaryButtonGradientBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="FocusedToggled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadPrimaryBrush}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="{DynamicResource RadPrimaryButtonGradientBorderBrush}" />
            </VisualState.Setters>
            </VisualState>
            <VisualState Name="ToggledMouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadToggleButtonToggledMouseOverBackground}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="{DynamicResource RadPrimaryButtonMouseOverGradientBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="FocusedToggledMouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadToggleButtonToggledMouseOverBackground}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="{DynamicResource RadPrimaryButtonMouseOverGradientBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ToggledPressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadToggleButtonToggledPressedBackground}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="{DynamicResource RadPrimaryButtonPressedGradientBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="FocusedToggledPressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadToggleButtonToggledPressedBackground}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="{DynamicResource RadPrimaryButtonPressedGradientBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ToggledDisabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadPrimaryButtonDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadToggleButtonToggledDisabledBackground}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="{DynamicResource RadButtonBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Indeterminate" />
            <VisualState Name="FocusedIndeterminate" />

            <VisualState Name="IndeterminateMouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadBaseHoverBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="FocusedIndeterminateMouseOver" >
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadBaseHoverBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="IndeterminatePressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadButtonPressedTextColor}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadBaseActiveBrush}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="{DynamicResource RadToggleButtonBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="FocusedIndeterminatePressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadButtonPressedTextColor}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadBaseActiveBrush}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="{DynamicResource RadOnAppSurfaceColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="IndeterminateDisabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadBaseActiveBrush}" />
                    <Setter Property="telerikMauiControls:RadToggleButton.BorderBrush" Value="{DynamicResource RadToggleButtonBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <Style x:Key="RadToggleButtonStyle" TargetType="telerikMauiControls:RadToggleButton">
        <Setter Property="ControlTemplate" Value="{OnPlatform Default={StaticResource RadToggleButton_ControlTemplate}, WinUI={StaticResource RadToggleButton_ControlTemplate_WinUI}}" />
        <Setter Property="telerikTheming:RadEffects.RippleMode" Value="Pulse" />
        <Setter Property="telerikTheming:RadEffects.RippleColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha12}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnBaseColor}" />
        <Setter Property="Background" Value="{DynamicResource RadButtonBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadButtonBorderBrush}" />
        <Setter Property="BorderThickness" Value="{OnPlatform Default=0, MacCatalyst=0.5, WinUI=1}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=4, iOS=8, MacCatalyst=5}" />
        <Setter Property="Padding" Value="{OnPlatform Android='8, 7', iOS='10, 9.5', MacCatalyst='8, 1.5', WinUI='11, 5, 11, 6'}" />
        <Setter Property="HorizontalTextAlignment" Value="Center" />
        <Setter Property="VerticalTextAlignment" Value="Center" />
        <Setter Property="Shadow" Value="{OnPlatform Default={x:Null}, MacCatalyst={DynamicResource RadButtonShadow}}" />
        <Setter Property="VisualStateManager.VisualStateGroups"
                Value="{OnPlatform Android={StaticResource ToggleButtonVisualStates_Android},
                                   iOS={StaticResource ToggleButtonVisualStates_iOS},
                                   MacCatalyst={StaticResource ToggleButtonVisualStates_MacCatalyst},
                                   WinUI={StaticResource ToggleButtonVisualStates_WinUI}}" />
    </Style>
    <Style TargetType="telerikMauiControls:RadToggleButton" BasedOn="{StaticResource RadToggleButtonStyle}" />

    <Style x:Key="RadDropDownButtonDropDownIndicatorStyle" TargetType="Label">
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="{OnPlatform Default=12, WinUI=16}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="Text" Value="{x:Static telerikMaui:TelerikFont.IconDownArrow}" />
        <Setter Property="HorizontalTextAlignment" Value="Center" />
        <Setter Property="VerticalTextAlignment" Value="Center" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <ControlTemplate x:Key="RadDropDownButton_ControlTemplate">
        <telerikMauiControls:RadEffectsView x:Name="PART_EffectsView"
                                            BackgroundColor="{TemplateBinding BackgroundColor}"
                                            Background="{TemplateBinding Background}"
                                            BorderColor="{TemplateBinding BorderColor}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="{TemplateBinding CornerRadius, Converter={StaticResource CornerRadiusToThicknessConverter}}"
                                            Padding="{TemplateBinding Padding}">
            <Grid x:Name="PART_LayoutGrid"
                  ColumnDefinitions="*, Auto">
                <ContentPresenter x:Name="PART_ContentPresenter"
                                  telerikMauiControls:ContentPresenterUtils.Content="{TemplateBinding ActualContent}" />
                <Label x:Name="PART_DropDownIndicator"
                       AutomationId="PART_DropDownIndicator"
                       Grid.Column="1"
                       IsVisible="{TemplateBinding IsDropDownIndicatorVisible}"
                       Style="{TemplateBinding ActualDropDownIndicatorStyle}">
                    <Label.Triggers>
                        <DataTrigger TargetType="Label" Binding="{TemplateBinding IsOpen}" Value="True">
                            <Setter Property="Text" Value="{OnPlatform Default={x:Static telerikMaui:TelerikFont.IconUpArrow}, WinUI={x:Static telerikMaui:TelerikFont.IconDownArrow}}" />
                        </DataTrigger>
                        <DataTrigger TargetType="Label" Binding="{Binding DropDownIndicatorPosition, Source={RelativeSource TemplatedParent}}" Value="Right">
                            <Setter Property="Margin" Value="8, 0, 0, 0" />
                        </DataTrigger>
                        <DataTrigger TargetType="Label" Binding="{Binding DropDownIndicatorPosition, Source={RelativeSource TemplatedParent}}" Value="Left">
                            <Setter Property="Margin" Value="0, 0, 8, 0" />
                        </DataTrigger>
                        <DataTrigger TargetType="Label" Binding="{Binding DropDownIndicatorPosition, Source={RelativeSource TemplatedParent}}" Value="Top">
                            <Setter Property="Margin" Value="0, 0, 0, 8" />
                        </DataTrigger>
                        <DataTrigger TargetType="Label" Binding="{Binding DropDownIndicatorPosition, Source={RelativeSource TemplatedParent}}" Value="Bottom">
                            <Setter Property="Margin" Value="0, 8, 0, 0" />
                        </DataTrigger>
                    </Label.Triggers>
                </Label>
            </Grid>
        </telerikMauiControls:RadEffectsView>
    </ControlTemplate>

    <ControlTemplate x:Key="RadDropDownButton_ControlTemplate_WinUI">
        <Grid>
            <telerikMauiControls:RadBorder InputTransparent="True"
                                           IsVisible="{TemplateBinding IsKeyboardFocused}"
                                           BorderColor="{DynamicResource RadOnAppSurfaceColor}"
                                           BorderThickness="2"
                                           CornerRadius="{TemplateBinding CornerRadius, Converter={StaticResource CornerRadiusToThicknessConverter}}"
                                           Margin="-3"
                                           telerikMauiControls:KeyboardHelper.IsTabStop="{TemplateBinding telerikMauiControls:KeyboardHelper.IsTabStop}" />
            <telerikMauiControls:RadEffectsView x:Name="PART_EffectsView"
                                                BackgroundColor="{TemplateBinding BackgroundColor}"
                                                Background="{TemplateBinding Background}"
                                                BorderColor="{TemplateBinding BorderColor}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="{TemplateBinding CornerRadius, Converter={StaticResource CornerRadiusToThicknessConverter}}"
                                                Padding="{TemplateBinding Padding}">
                <Grid x:Name="PART_LayoutGrid"
                      ColumnDefinitions="*, Auto">
                    <ContentPresenter x:Name="PART_ContentPresenter"
                                      telerikMauiControls:ContentPresenterUtils.Content="{TemplateBinding ActualContent}" />
                    <Label x:Name="PART_DropDownIndicator"
                           AutomationId="PART_DropDownIndicator"
                           Grid.Column="1"
                           IsVisible="{TemplateBinding IsDropDownIndicatorVisible}"
                           Style="{TemplateBinding ActualDropDownIndicatorStyle}">
                        <Label.Triggers>
                            <DataTrigger TargetType="Label" Binding="{TemplateBinding IsOpen}" Value="True">
                                <Setter Property="Text" Value="{OnPlatform Default={x:Static telerikMaui:TelerikFont.IconUpArrow}, WinUI={x:Static telerikMaui:TelerikFont.IconDownArrow}}" />
                            </DataTrigger>
                            <DataTrigger TargetType="Label" Binding="{Binding DropDownIndicatorPosition, Source={RelativeSource TemplatedParent}}" Value="Right">
                                <Setter Property="Margin" Value="8, 0, 0, 0" />
                            </DataTrigger>
                            <DataTrigger TargetType="Label" Binding="{Binding DropDownIndicatorPosition, Source={RelativeSource TemplatedParent}}" Value="Left">
                                <Setter Property="Margin" Value="0, 0, 8, 0" />
                            </DataTrigger>
                            <DataTrigger TargetType="Label" Binding="{Binding DropDownIndicatorPosition, Source={RelativeSource TemplatedParent}}" Value="Top">
                                <Setter Property="Margin" Value="0, 0, 0, 8" />
                            </DataTrigger>
                            <DataTrigger TargetType="Label" Binding="{Binding DropDownIndicatorPosition, Source={RelativeSource TemplatedParent}}" Value="Bottom">
                                <Setter Property="Margin" Value="0, 8, 0, 0" />
                            </DataTrigger>
                        </Label.Triggers>
                    </Label>
                </Grid>
            </telerikMauiControls:RadEffectsView>
        </Grid>
    </ControlTemplate>

    <VisualStateGroupList x:Key="DropDownButtonVisualStates_Android">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Pressed" />
             <VisualState Name="Opened">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadDropDownButton.Background" Value="{DynamicResource RadDropDownButtonOpenedBackgroundColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadDropDownButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="DropDownButtonVisualStates_iOS">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadDropDownButton.TextColor" Value="{DynamicResource RadDropDownButtonPressedTextColor}" />
                </VisualState.Setters>
            </VisualState>
             <VisualState Name="Opened">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadDropDownButton.Background" Value="{DynamicResource RadDropDownButtonOpenedBackgroundColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadDropDownButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="DropDownButtonVisualStates_MacCatalyst">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="MouseOver" />
            <VisualState Name="FocusedMouseOver" />
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadDropDownButtonPressedTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="FocusedPressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadDropDownButtonPressedTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Opened">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadDropDownButton.Background" Value="{DynamicResource RadDropDownButtonOpenedBackgroundColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadDropDownButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="DropDownButtonVisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="MouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadDropDownButton.Background" Value="{DynamicResource RadBaseHoverBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="FocusedMouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadDropDownButton.Background" Value="{DynamicResource RadBaseHoverBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadDropDownButton.TextColor" Value="{DynamicResource RadDropDownButtonPressedTextColor}" />
                    <Setter Property="telerikMauiControls:RadDropDownButton.Background" Value="{DynamicResource RadBaseActiveBrush}" />
                    <Setter Property="telerikMauiControls:RadDropDownButton.BorderBrush" Value="{DynamicResource RadDropDownButtonBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="FocusedPressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadDropDownButton.TextColor" Value="{DynamicResource RadDropDownButtonPressedTextColor}" />
                    <Setter Property="telerikMauiControls:RadDropDownButton.Background" Value="{DynamicResource RadBaseActiveBrush}" />
                    <Setter Property="telerikMauiControls:RadDropDownButton.BorderBrush" Value="{DynamicResource RadDropDownButtonBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Opened">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadDropDownButton.Background" Value="{DynamicResource RadDropDownButtonOpenedBackgroundColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadDropDownButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadDropDownButton.Background" Value="{DynamicResource RadBaseActiveBrush}" />
                    <Setter Property="telerikMauiControls:RadDropDownButton.BorderBrush" Value="{DynamicResource RadDropDownButtonBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <Style x:Key="RadDropDownButtonStyle" TargetType="telerikMauiControls:RadDropDownButton">
        <Setter Property="ControlTemplate" Value="{OnPlatform Default={StaticResource RadDropDownButton_ControlTemplate}, WinUI={StaticResource RadDropDownButton_ControlTemplate_WinUI}}" />
        <Setter Property="telerikTheming:RadEffects.RippleMode" Value="Pulse" />
        <Setter Property="telerikTheming:RadEffects.RippleColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha12}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="Background" Value="{DynamicResource RadDropDownButtonBackgroundColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadDropDownButtonBorderBrush}" />
        <Setter Property="DropDownBackgroundColor" Value="{DynamicResource RadDropDownButtonDropDownBackgroundColor}" />
        <Setter Property="DropDownBorderColor" Value="{DynamicResource RadDropDownButtonDropDownBorderColor}" />
        <Setter Property="Shadow" Value="{OnPlatform Default={x:Null}, MacCatalyst={DynamicResource RadButtonShadow}}" />
        <Setter Property="VisualStateManager.VisualStateGroups"
                Value="{OnPlatform Android={StaticResource DropDownButtonVisualStates_Android},
                                   iOS={StaticResource DropDownButtonVisualStates_iOS},
                                   MacCatalyst={StaticResource DropDownButtonVisualStates_MacCatalyst},
                                   WinUI={StaticResource DropDownButtonVisualStates_WinUI}}" />
        <Setter Property="DropDownIndicatorStyle" Value="{StaticResource RadDropDownButtonDropDownIndicatorStyle}" />
    </Style>
    <Style TargetType="telerikMauiControls:RadDropDownButton" BasedOn="{StaticResource RadDropDownButtonStyle}" />

    <ControlTemplate x:Key="RadSmartPasteButton_ControlTemplate">
        <telerikMauiControls:RadEffectsView x:Name="PART_EffectsView"
                                            BackgroundColor="{TemplateBinding BackgroundColor}"
                                            Background="{TemplateBinding Background}"
                                            BorderColor="{TemplateBinding BorderColor}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="{TemplateBinding CornerRadius, Converter={StaticResource CornerRadiusToThicknessConverter}}"
                                            Padding="{TemplateBinding Padding}">
            <Grid ColumnDefinitions="Auto, Auto"
                  HorizontalOptions="Center">
                <Label Text="{TemplateBinding IconText}"
                       TextColor="{TemplateBinding IconTextColor}"
                       FontFamily="{TemplateBinding IconFontFamily}"
                       FontSize="{TemplateBinding IconFontSize}"
                       FontAttributes="{TemplateBinding IconFontAttributes}"
                       IsVisible="{Binding IconText, Source={RelativeSource TemplatedParent}, Converter={StaticResource NullOrEmptyToBoolConverter}}"
                       HorizontalTextAlignment="End"
                       VerticalTextAlignment="Center">
                    <Label.Triggers>
                        <MultiTrigger TargetType="Label">
                            <MultiTrigger.Conditions>
                                <BindingCondition Binding="{Binding IconText, Source={RelativeSource TemplatedParent}, Converter={StaticResource NullOrEmptyToBoolConverter}}" Value="True" />
                                <BindingCondition Binding="{Binding Text, Source={RelativeSource TemplatedParent}, Converter={StaticResource NullOrEmptyToBoolConverter}}" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter Property="Margin" Value="{OnPlatform Default='0, 0, 9, 0', MacCatalyst='0, 0, 5, 0', WinUI='0, 0, 7, 0'}" />
                        </MultiTrigger>
                    </Label.Triggers>
                </Label>
                <Label Grid.Column="1"
                       Text="{TemplateBinding Text}"
                       TextColor="{TemplateBinding TextColor}"
                       FontFamily="{TemplateBinding FontFamily}"
                       FontSize="{TemplateBinding FontSize}"
                       FontAttributes="{TemplateBinding FontAttributes}"
                       TextDecorations="{TemplateBinding TextDecorations}"
                       VerticalTextAlignment="Center"
                       IsVisible="{Binding Text, Source={RelativeSource TemplatedParent}, Converter={StaticResource NullOrEmptyToBoolConverter}}" />
            </Grid>
        </telerikMauiControls:RadEffectsView>
    </ControlTemplate>

    <ControlTemplate x:Key="RadSmartPasteButton_ControlTemplate_WinUI">
        <Grid>
            <telerikMauiControls:RadBorder InputTransparent="True"
                                           IsVisible="{TemplateBinding IsKeyboardFocused}"
                                           BorderColor="{DynamicResource RadOnAppSurfaceColor}"
                                           BorderThickness="2"
                                           CornerRadius="{TemplateBinding CornerRadius, Converter={StaticResource CornerRadiusToThicknessConverter}}"
                                           Margin="-3"
                                           telerikMauiControls:KeyboardHelper.IsTabStop="{TemplateBinding telerikMauiControls:KeyboardHelper.IsTabStop}" />
            <telerikMauiControls:RadEffectsView x:Name="PART_EffectsView"
                                                BackgroundColor="{TemplateBinding BackgroundColor}"
                                                Background="{TemplateBinding Background}"
                                                BorderColor="{TemplateBinding BorderColor}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="{TemplateBinding CornerRadius, Converter={StaticResource CornerRadiusToThicknessConverter}}"
                                                Padding="{TemplateBinding Padding}">
                <Grid ColumnDefinitions="Auto, Auto"
                      HorizontalOptions="Center">
                    <Label Text="{TemplateBinding IconText}"
                           TextColor="{TemplateBinding IconTextColor}"
                           FontFamily="{TemplateBinding IconFontFamily}"
                           FontSize="{TemplateBinding IconFontSize}"
                           FontAttributes="{TemplateBinding IconFontAttributes}"
                           IsVisible="{Binding IconText, Source={RelativeSource TemplatedParent}, Converter={StaticResource NullOrEmptyToBoolConverter}}"
                           HorizontalTextAlignment="End"
                           VerticalTextAlignment="Center">
                        <Label.Triggers>
                            <MultiTrigger TargetType="Label">
                                <MultiTrigger.Conditions>
                                    <BindingCondition Binding="{Binding IconText, Source={RelativeSource TemplatedParent}, Converter={StaticResource NullOrEmptyToBoolConverter}}" Value="True" />
                                    <BindingCondition Binding="{Binding Text, Source={RelativeSource TemplatedParent}, Converter={StaticResource NullOrEmptyToBoolConverter}}" Value="True" />
                                </MultiTrigger.Conditions>
                                <Setter Property="Margin" Value="{OnPlatform Default='0, 0, 9, 0', MacCatalyst='0, 0, 5, 0', WinUI='0, 0, 7, 0'}" />
                            </MultiTrigger>
                        </Label.Triggers>
                    </Label>
                    <Label Grid.Column="1"
                           Text="{TemplateBinding Text}"
                           TextColor="{TemplateBinding TextColor}"
                           FontFamily="{TemplateBinding FontFamily}"
                           FontSize="{TemplateBinding FontSize}"
                           FontAttributes="{TemplateBinding FontAttributes}"
                           TextDecorations="{TemplateBinding TextDecorations}"
                           VerticalTextAlignment="Center"
                           IsVisible="{Binding Text, Source={RelativeSource TemplatedParent}, Converter={StaticResource NullOrEmptyToBoolConverter}}" />
                </Grid>
            </telerikMauiControls:RadEffectsView>
        </Grid>
    </ControlTemplate>
    
    <VisualStateGroupList x:Key="SmartPasteButtonVisualStates_Android">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Pressed" />
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.IconTextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Focused" />
            <VisualState Name="Processing">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.Text" Value="{telerikMauiControls:Localize SmartPasteButton_CancelText}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.IconText" Value="{x:Static telerikMauiCore:TelerikFont.IconCancel}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ProcessingPressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.Text" Value="{telerikMauiControls:Localize SmartPasteButton_CancelText}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.IconText" Value="{x:Static telerikMauiCore:TelerikFont.IconCancel}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ProcessingFocused" />
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="SmartPasteButtonVisualStates_iOS">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.TextColor" Value="{DynamicResource RadButtonPressedTextColor}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.IconTextColor" Value="{DynamicResource RadButtonPressedTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.IconTextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Focused" />
            <VisualState Name="Processing">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.Text" Value="{telerikMauiControls:Localize SmartPasteButton_CancelText}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.IconText" Value="{x:Static telerikMauiCore:TelerikFont.IconCancel}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ProcessingPressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.Text" Value="{telerikMauiControls:Localize SmartPasteButton_CancelText}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.IconText" Value="{x:Static telerikMauiCore:TelerikFont.IconCancel}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.TextColor" Value="{DynamicResource RadButtonPressedTextColor}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.IconTextColor" Value="{DynamicResource RadButtonPressedTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ProcessingFocused" />
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="SmartPasteButtonVisualStates_MacCatalyst">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="MouseOver" />
            <VisualState Name="FocusedMouseOver" />
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.TextColor" Value="{DynamicResource RadButtonPressedTextColor}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.IconTextColor" Value="{DynamicResource RadButtonPressedTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="FocusedPressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.TextColor" Value="{DynamicResource RadButtonPressedTextColor}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.IconTextColor" Value="{DynamicResource RadButtonPressedTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.IconTextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Focused" />
            <VisualState Name="Processing">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.Text" Value="{telerikMauiControls:Localize SmartPasteButton_CancelText}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.IconText" Value="{x:Static telerikMauiCore:TelerikFont.IconCancel}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ProcessingPressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.Text" Value="{telerikMauiControls:Localize SmartPasteButton_CancelText}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.IconText" Value="{x:Static telerikMauiCore:TelerikFont.IconCancel}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.TextColor" Value="{DynamicResource RadButtonPressedTextColor}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.IconTextColor" Value="{DynamicResource RadButtonPressedTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ProcessingMouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.Text" Value="{telerikMauiControls:Localize SmartPasteButton_CancelText}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.IconText" Value="{x:Static telerikMauiCore:TelerikFont.IconCancel}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ProcessingFocused" />
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="SmartPasteButtonVisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="MouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.Background" Value="{DynamicResource RadBaseHoverBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="FocusedMouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.Background" Value="{DynamicResource RadBaseHoverBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.TextColor" Value="{DynamicResource RadOnBaseColorAlpha61}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.IconTextColor" Value="{DynamicResource RadOnBaseColorAlpha61}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.Background" Value="{DynamicResource RadBaseActiveBrush}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.BorderBrush" Value="{DynamicResource RadToggleButtonBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="FocusedPressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.TextColor" Value="{DynamicResource RadOnBaseColorAlpha61}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.IconTextColor" Value="{DynamicResource RadOnBaseColorAlpha61}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.Background" Value="{DynamicResource RadBaseActiveBrush}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.BorderBrush" Value="{DynamicResource RadToggleButtonBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.IconTextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.Background" Value="{DynamicResource RadBaseActiveBrush}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.BorderBrush" Value="{DynamicResource RadToggleButtonBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Focused" />
            <VisualState Name="Processing">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.Text" Value="{telerikMauiControls:Localize SmartPasteButton_CancelText}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.IconText" Value="{x:Static telerikMauiCore:TelerikFont.IconCancel}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ProcessingPressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.Text" Value="{telerikMauiControls:Localize SmartPasteButton_CancelText}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.IconText" Value="{x:Static telerikMauiCore:TelerikFont.IconCancel}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.TextColor" Value="{DynamicResource RadOnBaseColorAlpha61}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.IconTextColor" Value="{DynamicResource RadOnBaseColorAlpha61}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.Background" Value="{DynamicResource RadBaseActiveBrush}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.BorderBrush" Value="{DynamicResource RadToggleButtonBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ProcessingMouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.Text" Value="{telerikMauiControls:Localize SmartPasteButton_CancelText}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.IconText" Value="{x:Static telerikMauiCore:TelerikFont.IconCancel}" />
                    <Setter Property="telerikMauiControls:RadSmartPasteButton.Background" Value="{DynamicResource RadBaseHoverBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ProcessingFocused" />
        </VisualStateGroup>
    </VisualStateGroupList>

    <Style x:Key="RadSmartPasteButtonStyle" TargetType="telerikMauiControls:RadSmartPasteButton">
        <Setter Property="ControlTemplate" Value="{OnPlatform Default={StaticResource RadSmartPasteButton_ControlTemplate}, WinUI={StaticResource RadSmartPasteButton_ControlTemplate_WinUI}}" />
        <Setter Property="telerikTheming:RadEffects.RippleMode" Value="Pulse" />
        <Setter Property="telerikTheming:RadEffects.RippleColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha12}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnBaseColor}" />
        <Setter Property="Background" Value="{DynamicResource RadButtonBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadButtonBorderBrush}" />
        <Setter Property="IconTextColor" Value="{DynamicResource RadOnBaseColor}" />
        <Setter Property="Shadow" Value="{OnPlatform Default={x:Null}, MacCatalyst={DynamicResource RadButtonShadow}}" />
        <Setter Property="VisualStateManager.VisualStateGroups"
                Value="{OnPlatform Android={StaticResource SmartPasteButtonVisualStates_Android},
                                   iOS={StaticResource SmartPasteButtonVisualStates_iOS},
                                   MacCatalyst={StaticResource SmartPasteButtonVisualStates_MacCatalyst},
                                   WinUI={StaticResource SmartPasteButtonVisualStates_WinUI}}" />
    </Style>
    <Style TargetType="telerikMauiControls:RadSmartPasteButton" BasedOn="{StaticResource RadSmartPasteButtonStyle}" />
</ResourceDictionary>