<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMaui="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Platform.Styles.InputView">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <Style x:Key="DefaultInputViewStyle" TargetType="InputView" ApplyToDerivedTypes="True">
        <!-- These following two properties are bound to the whole control's Minimum properties. This might lead to a larger size due to the presence of additional elements in the template.
             Consider exposing TextInputStyle or specifically TextInputMinimumHeightRequest and TextInputMinimumWidthRequest. See #85779. -->
        <Setter Property="Keyboard" Value="{TemplateBinding Keyboard}" />
        <Setter Property="IsSpellCheckEnabled" Value="{TemplateBinding IsSpellCheckEnabled}" />
        <Setter Property="IsTextPredictionEnabled" Value="{TemplateBinding IsTextPredictionEnabled}" />
        <Setter Property="IsReadOnly" Value="{TemplateBinding IsReadOnly}" />
        <Setter Property="CharacterSpacing" Value="{TemplateBinding CharacterSpacing}" />
        <Setter Property="TextTransform" Value="{TemplateBinding TextTransform}" />
        <Setter Property="FontAttributes" Value="{TemplateBinding FontAttributes}" />
        <Setter Property="FontFamily" Value="{TemplateBinding FontFamily}" />
        <Setter Property="FontSize" Value="{TemplateBinding FontSize}" />
        <Setter Property="FontAutoScalingEnabled" Value="{TemplateBinding FontAutoScalingEnabled}" />
        <Setter Property="MaxLength" Value="{TemplateBinding MaxLength}" />
        <Setter Property="Placeholder" Value="{TemplateBinding Placeholder}" />
    </Style>
    
    <telerikMauiControls:DictionaryConverter x:Key="ErrorImageOpacityConverter">
        <telerikMauiControls:DictionaryConverterItem Key="{x:Boolean True}" Value="{x:Double 0}" />
        <telerikMauiControls:DictionaryConverterItem Key="{x:Boolean False}" Value="{x:Double 1}" />
    </telerikMauiControls:DictionaryConverter>

    <FontImageSource x:Key="ErrorImageSource"
                     FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                     Glyph="{x:Static telerikMaui:TelerikFont.IconError}" />

    <VisualStateGroupList x:Key="CloseButton_VisualStates">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadSubtleColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Focused" />
            <VisualState Name="MouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadSubtleColor}" />
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource ClearButtonPointerOverBackground}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="FocusedMouseOver" />
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadInputViewActionButtonPressedTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="FocusedPressed" />
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadInputViewDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="CloseButton_VisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadSubtleColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Focused" />
            <VisualState Name="MouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadSubtleColor}" />
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource ClearButtonPointerOverBackground}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="FocusedMouseOver" />
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha45}" />
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource ClearButtonPressedBackground}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="FocusedPressed" />
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha36}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <Style x:Key="DefaultClearButtonStyle" TargetType="telerikMauiControls:RadTemplatedButton">
        <Setter Property="Grid.Column" Value="1" />
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Default=22, Android=20, iOS=24}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=22, Android=20, iOS=34}" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="{OnPlatform Default=19, Android=20, MacCatalyst=18, WinUI=10}" />
        <Setter Property="TextColor" Value="{DynamicResource RadSubtleColor}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Content" Value="{OnPlatform Default={x:Static telerikMaui:TelerikFont.IconCancel2}, WinUI={x:Static telerikMaui:TelerikFont.IconCancel}}" />
        <Setter Property="Command" Value="{TemplateBinding ActualClearButtonCommand}" />
        <Setter Property="IsVisible" Value="{TemplateBinding ActualIsClearButtonVisible}" />
        <Setter Property="Padding" Value="{OnPlatform Default=0, iOS='0, 0, 10, 0', MacCatalyst='0, 0, 10, 0'}" />
        <Setter Property="Margin" Value="{OnPlatform Default=0, Android='0, 0, 8, 0', WinUI='4, 0'}" />
        <Setter Property="VisualStateManager.VisualStateGroups" Value="{OnPlatform Default={StaticResource CloseButton_VisualStates}, WinUI={StaticResource CloseButton_VisualStates_WinUI}}" />
    </Style>

    <Style x:Key="DefaultValidationErrorImageStyle" TargetType="Image">
        <Setter Property="WidthRequest" Value="{OnPlatform Default=16, Android=18, iOS=20}" />
        <Setter Property="HeightRequest" Value="{OnPlatform Default=16, Android=18, iOS=20}" />
        <Setter Property="Source" Value="{StaticResource ErrorImageSource}" />
        <Setter Property="telerikMauiControls:FontImageSourceUtils.FallbackColor" Value="{DynamicResource RadErrorColor}" />
        <Setter Property="ToolTipProperties.Text" Value="{TemplateBinding ActualValidationErrorMessage}" />
        <Setter Property="IsVisible" Value="{TemplateBinding ActualIsValidationErrorImageVisible}" />
        <Setter Property="Opacity" Value="{TemplateBinding IsValueValid, Converter={StaticResource ErrorImageOpacityConverter}}" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="Margin" Value="{OnPlatform Android='4.5, 0, 0, 0.5', iOS='8, 0, 0, 0', MacCatalyst='6, 0, 0, 0', WinUI='8, 1, 0, 0'}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="Focused" />
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:FontImageSourceUtils.FallbackColor" Value="{DynamicResource RadInputViewDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="DefaultValidationErrorLabelStyle" TargetType="Label">
        <Setter Property="Text" Value="{TemplateBinding ValidationErrorMessage}" />
        <Setter Property="TextColor" Value="{DynamicResource RadErrorColor}" />
        <Setter Property="IsVisible" Value="{TemplateBinding ActualIsValidationErrorLabelVisible}" />
        <Setter Property="Opacity" Value="{TemplateBinding IsValueValid, Converter={StaticResource ErrorImageOpacityConverter}}" />
        <Setter Property="Margin" Value="{OnPlatform Android='8.5, 4, 0, 0', iOS='8, 4, 0, 0', MacCatalyst='6, 4, 0, 0', WinUI='10, 4, 0, 0'}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="Focused" />
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadInputViewDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <VisualStateGroupList x:Key="RadInputView_VisualStates_Android">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Focused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BackgroundColor" Value="{DynamicResource RadInputViewFocusedBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewFocusedBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderThickness" Value="0, 0, 0, 2" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Invalid">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewInvalidBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="InvalidFocused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BackgroundColor" Value="{DynamicResource RadInputViewFocusedBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewInvalidFocusedBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderThickness" Value="0, 0, 0, 2" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnly" />
            <VisualState Name="ReadOnlyFocused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BackgroundColor" Value="{DynamicResource RadInputViewFocusedBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewReadOnlyFocusedBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderThickness" Value="0, 0, 0, 2" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnlyInvalid">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewInvalidBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnlyInvalidFocused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewInvalidFocusedReadOnlyBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderThickness" Value="0, 0, 0, 2" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.PlaceholderColor" Value="{DynamicResource RadInputViewDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.TextColor" Value="{DynamicResource RadInputViewDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewDisabledBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="RadInputView_VisualStates_iOS">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Focused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BackgroundColor" Value="{DynamicResource RadInputViewFocusedBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewFocusedBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Invalid">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewInvalidBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="InvalidFocused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BackgroundColor" Value="{DynamicResource RadInputViewFocusedBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewInvalidFocusedBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnly" />
            <VisualState Name="ReadOnlyFocused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BackgroundColor" Value="{DynamicResource RadInputViewFocusedBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewReadOnlyFocusedBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnlyInvalid">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewInvalidBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnlyInvalidFocused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewInvalidFocusedReadOnlyBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.PlaceholderColor" Value="{DynamicResource RadInputViewDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.TextColor" Value="{DynamicResource RadInputViewDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.BackgroundColor" Value="{DynamicResource RadInputViewDisabledBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewDisabledBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="RadInputView_VisualStates_MacCatalyst">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="MouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BackgroundColor" Value="{DynamicResource RadInputViewBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderThickness" Value="1" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Focused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BackgroundColor" Value="{DynamicResource RadInputViewFocusedBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewFocusedBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderThickness" Value="3" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Invalid">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewInvalidBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="InvalidMouseOver" />
            <VisualState Name="InvalidFocused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BackgroundColor" Value="{DynamicResource RadInputViewFocusedBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewInvalidFocusedBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderThickness" Value="3" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnly" />
            <VisualState Name="ReadOnlyMouseOver" />
            <VisualState Name="ReadOnlyFocused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BackgroundColor" Value="{DynamicResource RadInputViewFocusedBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewReadOnlyFocusedBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderThickness" Value="3" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnlyInvalid">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewInvalidBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnlyInvalidMouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewInvalidBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnlyInvalidFocused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewInvalidFocusedReadOnlyBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderThickness" Value="3" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.PlaceholderColor" Value="{DynamicResource RadInputViewDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.TextColor" Value="{DynamicResource RadInputViewDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.BackgroundColor" Value="{DynamicResource RadInputViewDisabledBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewDisabledBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="RadInputView_VisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="MouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BackgroundColor" Value="{DynamicResource RadBaseHoverColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderThickness" Value="1" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Focused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BackgroundColor" Value="{DynamicResource RadInputViewFocusedBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewFocusedBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderThickness" Value="1, 1, 1, 2" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Invalid">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewInvalidBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="InvalidMouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BackgroundColor" Value="{DynamicResource RadBaseHoverColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewInvalidBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="InvalidFocused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BackgroundColor" Value="{DynamicResource RadInputViewFocusedBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewInvalidFocusedBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderThickness" Value="1, 1, 1, 2" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnly">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BackgroundColor" Value="{DynamicResource RadInputViewReadOnlyBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource ReadOnlyBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnlyMouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BackgroundColor" Value="{DynamicResource RadBaseHoverColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource ReadOnlyBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnlyFocused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BackgroundColor" Value="{DynamicResource RadInputViewFocusedBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewReadOnlyFocusedBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderThickness" Value="1, 1, 1, 2" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnlyInvalid">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BackgroundColor" Value="{DynamicResource RadInputViewReadOnlyBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewInvalidBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnlyInvalidMouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BackgroundColor" Value="{DynamicResource RadBaseHoverColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewInvalidBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnlyInvalidFocused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.BackgroundColor" Value="{DynamicResource RadInputViewReadOnlyBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewInvalidFocusedReadOnlyBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderThickness" Value="1, 1, 1, 2" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadInputView.PlaceholderColor" Value="{DynamicResource RadInputViewDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.TextColor" Value="{DynamicResource RadInputViewDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.BackgroundColor" Value="{DynamicResource RadInputViewDisabledBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadInputView.BorderBrush" Value="{DynamicResource RadInputViewDisabledBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <Style x:Key="RadInputViewStyle" TargetType="telerikMauiControls:RadInputView" ApplyToDerivedTypes="True">
        <Setter Property="ClearButtonStyle" Value="{StaticResource DefaultClearButtonStyle}" />
        <Setter Property="ValidationErrorImageStyle" Value="{StaticResource DefaultValidationErrorImageStyle}" />
        <Setter Property="ValidationErrorLabelStyle" Value="{StaticResource DefaultValidationErrorLabelStyle}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=32, Android=44, iOS=36}" />
        <Setter Property="PlaceholderColor" Value="{DynamicResource RadSubtleColor}" />
        <Setter Property="TextColor" Value="{DynamicResource RadInputViewTextColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadInputViewBackgroundColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadInputViewBorderBrush}" />
        <Setter Property="BorderThickness" Value="{OnPlatform Default=1, Android='0, 0, 0, 1'}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Android='4, 4, 0, 0', iOS=10, MacCatalyst=6, WinUI=4}" />
        <Setter Property="ValidationErrorColor" Value="{DynamicResource RadErrorColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups"
                Value="{OnPlatform Android={StaticResource RadInputView_VisualStates_Android},
                                   iOS={StaticResource RadInputView_VisualStates_iOS},
                                   MacCatalyst={StaticResource RadInputView_VisualStates_MacCatalyst},
                                   WinUI={StaticResource RadInputView_VisualStates_WinUI}}" />
    </Style>
</ResourceDictionary>
